# CLAUDE.md

새 세션에서 이 프로젝트를 작업할 때 읽는 안내 문서입니다.

## 시작하기 전에

**`기획.md`를 먼저 전부 읽으십시오.** 게임 설계, 적 14종 데이터, 스킬 14종 데이터, 구현 순서가 전부 그 문서에 있습니다. 이 CLAUDE.md는 현재 상태와 다음 할 일만 알려주는 짧은 안내입니다.

## 프로젝트 성격

개인 프로젝트입니다. 학원 업무와 무관하며 직접 만들고 즐기는 것이 목표입니다. 상용 출시 계획은 없으므로 과도한 엔지니어링보다 "플레이해보고 재밌는지 확인하며 붙여나가는" 방식으로 진행합니다.

## 기술 스택

- TypeScript + Canvas 2D + Vite (게임 엔진 없음)
- 배포는 GitHub Pages 예정 (`vite.config.ts`의 `base: './'`)
- 렌더링은 `src/render/renderer.ts` 인터페이스로 분리되어 있습니다. PixiJS로 바꾸려면 이 파일의 구현만 교체하면 됩니다

## 현재 진행 상태 (2026-08-04)

**기획.md 9장의 1~8단계를 전부 구현했습니다.** 적 14종 + 정예 + 보스 3종, 스킬 14종 + 기본공격, 성장, 난도 곡선, 난이도 선택, 코인, 상점 3탭, 도감, 기록, 파티클과 화면 흔들림까지 들어가 있습니다.

**첫 플레이 피드백을 반영했습니다 (2026-08-04 ~ 08-05).** 아래 여덟 가지가 그 뒤에 들어갔습니다.

1. **정예 식별 강화**: 크기 1.15배 → 1.3배, 붉은 이중 링 + 바닥 오라 + 머리 위 뿔 + 항상 보이는 체력바 (`render/scene.ts`의 `drawEliteMark`, `ELITE.markColor`)
2. **쓸모없는 스탯을 레벨업 추첨에서 제외**: 투사체 속도 · 피격 무적 시간 · 코인 획득 범위. `STAT_DEFS`의 `rollable: false`로 표시하며, 상점 영구 강화로는 여전히 오릅니다
3. **레벨업 시 스탯 2개 상승** (`STAT_GAINS_PER_LEVEL`). 서로 다른 스탯이 뽑힙니다
4. **난이도 시스템**: **-1 부터 15까지**입니다(2026-08-06 재설계). 선택 화면은 처음부터 열리고 -1·0 은 언제나 고를 수 있습니다. 1 부터는 아래 난이도로 요구 시간을 버텨야 차례로 열립니다(0~2는 15분, **3부터는 30분**). 효과는 누적이며 표는 `DIFFICULTY_STEPS`에 있습니다. 계산은 `meta/difficulty.ts`, 화면은 `ui/screens/difficulty.ts`
5. **사망 원인 표시**: 게임오버 화면 우측 상단에 나를 죽인 적의 생김새와 이름이 뜹니다. 적탄 · 장판 · 자폭도 주인을 추적합니다 (`KillerInfo`, `World.killedBy`)
6. **적 14종 색·모양 전면 재배치**: "자폭적과 기본적이 구분되지 않는다"는 지적에서 출발했습니다. 둘 다 붉은 원이었을 뿐 아니라, 원형이 5종에 붉은 계열이 4종이었고 가장 안 위험한 기본적이 가장 강렬한 빨강을 쓰고 있었습니다. **색은 위험도, 모양은 행동 유형**으로 규칙을 세워 전부 다시 잡았습니다 (아래 "반드시 지킬 것" 4번)
7. **정예의 종류별 체력·속도** (2026-08-05): 탱커 정예는 속도 x0.8 · 체력 x5, 빠른적 정예는 속도 x1.2 · 체력 x2. 나머지는 공통 배율 그대로입니다 (`ELITE_TRAITS`, `eliteStatMul`)
8. **자폭적 스폰 후 1초 대기** (2026-08-05): 가장자리에서 나오자마자 붙던 것을 잠시 멈춰 세웠습니다. 대기 중에는 몸 주변 링이 조여듭니다 (`ENEMY_PARAMS.bomber.spawnDelay`)

**밸런스는 여전히 플레이로 확인해야 합니다.** 난이도 표의 수치도 자동 검증만 거친 초안입니다.

들어가지 않은 것: 사운드.

## 실행

```
npm install
npm run dev          # 개발 서버 (브라우저 자동 실행)
npm run build        # 타입 검사 + 프로덕션 빌드
npm run preview      # 빌드 결과 확인
npm run check        # 타입 검사 + 로직 점검 + 부팅 점검 (브라우저 불필요)
npm run standalone   # 남에게 건네줄 단일 HTML 생성 → ../피하기게임-배포/
```

`npm run standalone` 은 게임 전체를 **HTML 파일 하나**로 묶어 `../피하기게임-배포/` 에 넣습니다. 받는 사람은 더블클릭만 하면 되고 설치도 서버도 인터넷도 필요 없습니다.

Vite 기본 빌드로는 안 됩니다. `<script type="module" src=...>` 라서 `file://` 로 열면 브라우저가 CORS 로 막습니다. 그래서 esbuild 로 **IIFE 한 덩어리**를 만들어 HTML 안에 직접 넣습니다 (`scripts/standalone.mjs`). 이미지도 사운드도 없이 전부 도형으로 그리기 때문에 가능한 방법입니다. **자산을 추가하면 이 방식이 깨지므로 그때는 data URI 로 심어야 합니다.**

**배포본은 사용자가 갱신해달라고 할 때만 다시 만듭니다.** 코드를 고쳤다는 이유로 알아서 `npm run standalone` 을 돌리지 마십시오. 배포본 갱신은 빌드 단계가 아니라 "지금 이 상태를 남에게 준다"는 결정입니다. 검증 전이거나 밸런스를 만지는 중인 상태가 배포본에 들어가면 그대로 남에게 전달됩니다. 배포본이 낡았다는 사실만 알려주고 판단은 사용자에게 맡기십시오.

## 조작

- 이동: **방향키만** (WASD는 이동에 쓰지 않습니다)
- 스킬: **Q 하나뿐입니다.** 유틸 스킬은 항상 1개만 들 수 있고 그것만 손으로 씁니다. 공격 스킬은 3칸까지 모이고 전부 자동 발동입니다
- 일시정지: Esc
- 메뉴: 숫자키 또는 클릭. 레벨업 선택은 1/2/3, 리롤은 R
- **난이도 선택에는 숫자 단축키가 없습니다.** 가운데 숫자를 좌우 화살표(키 또는 클릭)로 돌리고 Enter 또는 시작 버튼으로 들어갑니다. 고른 난이도의 효과가 그 아래에 전부 펼쳐집니다

**조준 조작은 없습니다.** 기본공격과 스킬 모두 자동으로 대상을 고릅니다.

조작 안내는 **메인 화면 좌측 상단 `조작법` 버튼**에 접혀 있습니다. 마우스를 올리면 펴집니다. 예전에는 화면 아래에 두 줄로 늘 펼쳐져 있었는데, 한 번 읽으면 다시 볼 일이 없는 글이 자리를 차지하고 있었습니다.

**주소 파라미터 둘은 개발자 모드에서만 듣습니다** (아래 "개발자 모드" 참고).

- `?seed=12345` 시드가 고정되어 같은 상황이 재현됩니다 (기획 10장의 시드 고정 모드)
- `?unlock` 한 번 열면 **난이도 15까지 전부 열리고** 저장에 남습니다. 15단계까지 차례로 깨고 올라가면서 밸런스를 볼 수는 없어서 둔 통로입니다 (`main.ts` 의 `unlockAllFromUrl`)

**README 에는 이 둘을 적지 않았습니다.** 코드에 남겨두는 것과 문서에 적어두는 것은 노출 수준이 완전히 다릅니다. 링크를 받은 사람이 README 만 보고 쓸 수 있으면 잠근 의미가 없습니다.

## 개발자 모드 (2026-09-03)

**개발용 통로 넷이 전부 이 하나 뒤에 있습니다.** 디버그 오버레이(F1) · `?unlock` · `?seed` · 설정의 업적 초기화입니다. 판 중에 F1 을 눌러 비밀번호를 치면 켜지고, 설정 화면의 `개발자 모드 끄기` 로 끕니다.

- **저장에 남습니다** (`SaveData.devMode`). 탭이 아니라 브라우저 단위입니다. `?unlock` 은 페이지가 열릴 때, `?seed` 는 판이 시작될 때 읽히는데 그 시점에는 아직 디버그를 켤 기회가 없습니다. **잠금을 탭 안에만 두면 주소 파라미터는 영영 안 듣습니다**
- **새 저장은 반드시 꺼져 있어야 합니다.** 켜진 채로 시작하면 넷이 통째로 열립니다. `smoke.ts` 16번이 잡습니다
- **끄는 길이 반드시 있어야 합니다.** 켜기만 되고 못 끄면 한 번 켠 브라우저는 영영 개발자용입니다
- **설정 화면에서 개발자 항목은 켜져 있을 때만 보입니다.** 잠겨 있으면 목록에 아예 없습니다. 회색으로 흐려두면 그 자리에 무언가 있다는 사실을 알려주게 됩니다
- 규칙은 `scripts/boot.ts` 2번(잠겨 있으면 안 보임)과 6-2번(켠 뒤에는 보임)이 잡습니다

## 설정 화면

메인 화면 **우측 상단 톱니바퀴**(또는 6 키)로 들어갑니다. **안에서는 마우스로만 씁니다** (2026-09-03). 카드마다 붙어 있던 숫자 단축키를 뺐습니다. 설정은 판 중에 급하게 누르는 것이 아니라 한 번 들어와 만지고 나가는 자리라, 손이 키보드에 있어야 할 이유가 없습니다. **Esc 만 남겼는데 나가는 수단이 아니라 겨눈 것을 푸는 수단입니다.** 초기화를 잘못 눌러 겨눈 상태에서 마우스로 풀려면 다른 카드를 눌러야 하는데 그게 더 위험합니다.

**화면은 두 구역입니다.** 켜고 끄는 것과 되돌릴 수 없는 것을 같은 모양으로 나란히 두면 위험도가 뭉개집니다.

| 항목 | 값 | 비고 |
|---|---|---|
| 화면 흔들림 | 끔 / **절반** / 전체 | 기본이 절반이고 **그게 예전에 상수로 쓰던 세기입니다** |
| 파티클 | 끔 / 적게 / 보통 / **전체** | 기본이 전체이고 지금까지의 모습 그대로입니다 |
| 창을 벗어나면 정지 | **켬** / 끔 | 30분짜리 판이라 중간에 다른 창을 볼 일이 생깁니다 |

**전체화면은 넣었다가 뺐습니다** (2026-09-03). 두 가지가 걸렸습니다.

1. **여백이 사라지지 않습니다.** 이 게임은 1710 x 720, 즉 **2.375 : 1** 인데 모니터는 대개 16:9 입니다. 가로를 채우면 세로 25% 가 검은 띠로 남습니다. 게다가 창을 최대화한 상태와 전체화면은 **가로 폭이 같아서** 게임 크기가 아예 안 변합니다. 늘어난 세로가 전부 여백이 됩니다
2. **고치려고 `#stage` 로 캔버스와 메뉴를 한 덩어리로 묶어 배율을 걸었더니, 전체화면이 아닐 때도 화면이 우측으로 치우쳤습니다.** 되돌렸습니다

**여백을 없애려면 게임 크기 자체를 바꿔야 합니다.** 경기장을 세로로 키우거나(밸런스가 바뀝니다) 좌우 패널을 위아래로 옮기는 것(화면을 다시 짜야 합니다)뿐입니다. 지금은 하지 않기로 했습니다.

**하드모드는 히든 요소입니다** (2026-09-03, `SaveData.hardMode`).

**난이도 0 부터 15 까지 전부 클리어하면** 설정 맨 아래에 버튼이 나타납니다. 그전에는 자리조차 없습니다.

- **입문(-1)은 안 셉니다.** 일부러 쉽게 만든 난이도라 도전의 증거로 삼기에 어울리지 않습니다. **업적 "완주"(-1 포함)와 범위가 다르다는 것에 주의하십시오.** 같은 계산을 두 벌로 적지 않도록 `clearedAllFrom(save, from)`(`meta/difficulty.ts`) 하나에 시작점만 달리 넘깁니다. 두 벌로 두면 클리어 판정을 바꿀 때 한쪽만 고쳐서 "업적은 열렸는데 하드모드는 안 열리는" 일이 생깁니다
- **잠겨 있을 때 흐린 카드로 자리를 잡아두지 않습니다.** 그러면 히든이 아니라 "아직 못 여는 것"이 되어, 있는 줄 알고 조건을 찾게 됩니다
- **조건은 저장의 난이도별 최고 기록으로 봅니다.** 예전에 깬 것도 인정되고 한 번 열리면 다시 닫히지 않습니다
- 규칙은 `smoke.ts` 16번이 경계까지 잽니다 (한 단계라도 남으면 안 열림 · **1초라도 모자라면 안 열림** · 입문을 안 깨도 열림 · 업적 완주와는 다름)

**겉모습은 만들었습니다** (2026-09-03). 켜면 화면 전체에 붉은 기운이 은은하게 깔리고 메뉴 색도 붉은 쪽으로 조금 옮겨갑니다.

- **캔버스와 메뉴가 따로 놉니다.** 캔버스는 매 프레임 맨 위에 `HARD.tint` 를 덮고(`main.ts` 의 render 마지막 줄), 메뉴는 `body.hard` 가 CSS 변수를 옮깁니다. **켜고 끌 때 `applyHardTheme()` 를 부르십시오.** 데이터 초기화로 꺼질 때도 불러야 색만 붉게 남지 않습니다
- **덧칠은 화면 전체에 고르게 겁니다. 적 색을 따로 바꾸지 않습니다.** 이 게임은 "색이 위험도"라는 규칙으로 적 14종의 색을 잡아놨는데, 색마다 다르게 손대면 그 구분이 무너집니다. 고르게 덮으면 서로의 차이는 그대로 남습니다
- **덧칠은 은은해야 합니다.** 진하면 적탄(빨강)이 배경에 묻혀서 날아오는 것을 못 봅니다. `smoke.ts` 16번이 투명도가 0.15 이하인지 잽니다
- **CSS 는 있던 변수만 옮깁니다.** 화면마다 색을 따로 적기 시작하면 하드모드를 만질 때 모든 화면을 다시 훑어야 합니다. **`--danger` 와 `--ok` 는 그대로 둡니다.** 그 둘은 멋이 아니라 뜻을 나르는 색이라(위험 / 성공) 배경이 붉어졌다고 같이 옮기면 구분이 사라집니다

**⚠ 규칙은 아직 없습니다. 켜도 게임은 그대로입니다.** 아래가 사용자가 정해둔 방향이고, 만들 때 이걸 기준으로 삼으십시오 (2026-09-03).

> **하드모드에 들어가면 난이도는 똑같이 0 부터 15 로 올라갑니다. 다만 기존과 달리 일반모드의 내용이 0 부터 포함됩니다. -1(입문)은 없습니다.**

여기서 확실한 것과 아직 안 정한 것을 갈라 둡니다.

**확실한 것**

- **하드모드에도 난이도가 있고 범위는 0 ~ 15 입니다.** 하드모드가 난이도 하나로 취급되는 것이 아닙니다
- **하드모드에는 입문(-1)이 없습니다.** 난이도 화면의 왼쪽 끝이 0 이 됩니다
- **하드모드의 난이도 0 은 일반모드의 0 과 다릅니다.** 일반모드 0 은 "추가 효과 없음"인데, 하드모드 0 에는 일반모드의 내용이 이미 들어가 있습니다

**하드모드 0 = 일반 15 위에서 시작합니다** (2026-09-03 사용자 확정, 위 (가)안).

일반모드의 마지막 자리가 하드모드의 출발선입니다. 거기에 하드모드 표가 다시 쌓입니다.
출발선의 값은 `difficultyMods(15)` 그대로이고, 실제로 뽑아보면 이렇습니다.

| | 하드 0 의 출발값 |
|---|---|
| 배율 | 체력 x2.10 · 공격력 x1.70 · 접촉 x1.50 · 속도 x1.20 · 적탄 x1.10 · 스폰율 x1.50 · 장판 지속 x1.20 · 코인 x3.25 |
| 장치 | 전원 정예(표식 없음) · 스킬 선택지 2장 · 1분마다 15마리 웨이브 · 30초마다 자폭병 5마리 · 돌진 기절/쿨 없음 · 분열 탄막 · 무적 바보적 상시 · 바보적 8방향 |
| 클리어 | 30분 |

**여기서 곧바로 걸리는 문제가 있습니다. 하드모드 표는 `DIFFICULTY_STEPS` 를 복사해 쓸 수 없습니다.**

배율은 계속 더하면 되지만 **장치는 이미 전부 켜져 있습니다.** 전원 정예를 또 켤 수 없고,
무적 바보적도 이미 나와 있습니다. 갈아끼우는 항목(`wave` · `bomberWave`)만 값을 키울 수
있고 나머지는 켤 것이 남아 있지 않습니다. **선택지는 2장인데 여기서 더 줄이면 1장이 되고,
그건 "고른다"가 아니라 "받는다"가 되어 레벨업이라는 장치 자체가 사라집니다.**

그래서 하드모드 표를 만들 때는 **새 장치가 필요합니다.** 일반모드에 없던 규칙을 15칸에
새로 채우는 일이고, 배율만 더 올리는 표를 붙이면 "숫자만 큰 같은 게임"이 됩니다.
이건 하드모드를 만들 때의 가장 큰 작업입니다.

**아직 안 정한 것 (물어보고 만드십시오)**

- **해금은 어떻게 되는가.** 하드모드 안에서도 아래를 깨야 위가 열리는지, 아니면 처음부터 15까지 열려 있는지
- **기록과 업적을 따로 남기는가.** 같이 남기면 하드모드 기록이 일반 기록(`bestTimeByDifficulty`)을 덮어써서, 일반모드의 최고 기록이 사라집니다. **여기는 반드시 갈라야 합니다**
- **판 중에 켜고 끌 수 있는가.** 지금 스위치는 판 밖에서만 만지는 자리에 있습니다
- **하드모드가 켜졌다는 것을 게임 화면에서 알 수 있는가.** 지금은 붉은 기운뿐이라, 설정에서 켜놓고 잊으면 왜 어려운지 모릅니다

**반드시 지킬 것입니다.**

- **`Effects` 는 월드와 다른 난수기를 씁니다** (`world.ts` 에서 따로 만들어 넘깁니다). 같은 것을 쓰면 파티클을 줄인 사람은 난수를 덜 뽑게 되어 그 뒤의 스폰과 추첨이 통째로 밀립니다. **시드를 고정해도 설정마다 다른 판이 됩니다.** 눈에 보이는 것과 게임의 결과는 서로를 건드리면 안 됩니다. `smoke.ts` 16번이 두 설정으로 같은 판이 나오는지 직접 잽니다
- **`SHAKE.mul` 은 이제 "전체" 기준값(0.6)입니다.** 설정 기본값 절반(0.5)이 곱해져 실제로는 0.3 이고, 예전과 같은 세기입니다. 이 상수를 만질 때 설정 배율이 위에 곱해진다는 것을 잊지 마십시오
- **설정을 바꾸면 판이 도는 중에도 바로 먹입니다** (`main.ts` 가 `world.effects` 를 그 자리에서 고칩니다). 다음 판부터 적용되면 무엇이 바뀌었는지 확인할 방법이 없습니다
- **onclick 에 인자를 받는 함수를 그대로 넘기지 마십시오.** `gearButton(actions.settings)` 로 넘겨서 클릭 이벤트가 알림 문구 자리에 들어가고, 화면에 **`[object MouseEvent]`** 가 찍히고 있었습니다. 마우스로 톱니를 눌렀을 때만 나서 눈치채기 어려웠습니다. `boot.ts` 2번이 잡습니다

 카드 목록에는 넣지 않습니다. 목록은 "이 게임에서 할 것"이 늘어서는 자리인데 설정은 게임의 내용이 아니라 그 바깥이라, 상점·도감과 같은 무게로 서면 안 됩니다.

`ui/screens/settings.ts` 에 항목 셋이 있는데 **`모든 데이터 초기화` 하나만 늘 보입니다.** 나머지 둘은 개발자 모드에서만 나타납니다 (위 "개발자 모드" 참고). **전부 한 번 눌러 겨누고 한 번 더 눌러야 실행됩니다.** 되돌릴 방법이 없는데 카드 한 번에 지워지면 도감을 열려다 기록을 통째로 날립니다. Esc 는 겨눈 것을 먼저 풀고, 그다음 Esc 에 화면을 나갑니다.

- **모든 데이터 초기화**: `resetSave()`. 마지막에 고른 난이도(`difficulty`)도 0 으로 되돌립니다. 해금이 0 인데 12 가 남아 있으면 어긋납니다
- **업적 초기화** (`resetAchievements`, 개발자 모드 전용): **밸런스를 다 보고 나면 지울 항목입니다.** 코드에 `TEST_ONLY` 로 표시해 뒀습니다. 코인은 되돌리지 않고, 기록으로 판정되는 업적은 다음 판정에서 곧바로 다시 열리면서 코인이 또 들어옵니다. 알림을 다시 보려고 만든 버튼이라 그게 맞습니다. **다만 한 번 누를 때마다 8,100 코인이 들어옵니다.** 21번이면 영구 강화(172,295 코인)가 전부 채워지므로, 개발자 모드 밖으로 나가면 안 됩니다
- **개발자 모드 끄기** (개발자 모드 전용): 넷을 한꺼번에 잠급니다. 다시 켜려면 비밀번호를 쳐야 합니다

## 디버그 (F1, 잠금)

**F1 만으로는 안 열립니다.** 판 중에 F1 을 누르면 점 아홉 개짜리 화면이 뜨고, 정해진 순서를 쳐야 들어갑니다. 그 순서는 `DEBUG.unlockSequence`(`balance.ts`)에 있습니다. **여기에는 적지 않습니다.** 저장소가 공개라 문서에까지 적으면 열어보는 사람이 곧바로 봅니다.

- **잠금 화면에는 아무 글자도 없습니다** (`ui/screens/debugGate.ts`). "디버그"라고 써 두면, 잘못 눌러 들어온 사람에게 그 자리에 무언가 있다는 사실을 알려주게 됩니다. 판마다 바뀌지 않는 글은 안 적는다는 규칙(열세 번째 조정)과도 맞습니다
- **글자가 아니라 `KeyboardEvent.code` 를 셉니다.** 입력칸을 두면 한글 입력 상태에서 같은 자리를 눌러도 다른 글자가 들어가서, 입력기 상태에 따라 열리기도 하고 안 열리기도 합니다. `code` 는 자판의 자리라 그 영향을 안 받고 대소문자도 무관합니다
- **잠금 화면이 뜨는 동안 판이 멈춥니다.** `open('debugauth', ...)` 를 거치므로 `screen` 이 `playing` 이 아니게 되고 `world.update` 가 안 돕니다. 안 멈추면 비밀번호를 치는 동안 가만히 서서 맞아 죽습니다
- **한 번 풀면 그 탭이 살아 있는 동안 유지됩니다** (`Debug.unlocked`). 그 뒤로 F1 은 그냥 켜고 끕니다. 매번 다시 치게 하면 성가셔서 정작 필요할 때 안 쓰게 됩니다. 새로고침하면 다시 잠깁니다
- **이건 자물쇠가 아니라 문턱입니다.** 소스가 공개돼 있으므로 작정하고 뜯어보면 나옵니다. 막으려는 것은 "링크를 받아 들어온 사람이 F1 을 눌러보다 디버그를 켜는 일"입니다
- 규칙은 `scripts/boot.ts` 5번이 잡습니다 (F1 만으로는 안 열림 · 틀린 키로는 안 열림 · 순서를 치면 열림)

오버레이를 켜면 fps, 엔티티 수, 적 종류별 개수, 플레이어 스탯, 스킬 쿨다운, 현재 타겟이 보입니다. 오버레이가 켜져 있을 때만 아래 키가 동작합니다.

| 키 | 동작 |
|---|---|
| F2 / F3 | 단독 소환 대상 변경 / 소환 (Shift+F3은 정예) |
| F4 | 자동 스폰 on/off |
| B / K / P | 보스 소환 / 전멸 / 200마리 소환 |
| L / T / H / G | 강제 레벨업 / +60초 / 회복 / 무적 |
| X | 즉사 (부활이 남아 있으면 그만큼 더 눌러야 판이 끝납니다) |

## 검증 방법

`npm run check`가 브라우저 없이 세 가지를 돌립니다.

1. `npx tsc --noEmit` 타입 검사
2. `scripts/smoke.ts` 로직 점검: 14분 자동 플레이, 적 14종 단독 동작, 스킬 14종 발동, 방패적 처치 가능 여부, 레벨업 스탯 2개 추첨, 난이도 강화 반영, 사망 원인 기록, 적 200마리 성능
3. `scripts/boot.ts` 부팅 점검: 최소 DOM을 흉내 내어 메인 화면 → 상점 → 게임 시작 → 일시정지 → 레벨업 → 게임오버까지 키 입력으로 밟습니다

`npm run survival`은 회피에만 집중하는 봇으로 생존 시간을 잽니다 (`npm run survival -- 25 5` = 최대 25분, 시드 5개). 밸런스를 만진 뒤 생존 시간이 어떻게 움직였는지 보는 용도입니다. 봇은 사람보다 약하므로 절대 기준이 아니라 변화량을 보는 지표로만 쓰십시오. **시드마다 편차가 매우 크므로 3개로는 판단하지 마십시오.** 6개는 써야 중앙값이 흔들리지 않습니다.

`npm run levelcurve`는 **죽지 않는 봇**으로 30분을 완주시켜 "몇 분에 몇 레벨인가"와 "판당 스킬 선택이 몇 번 오는가"를 잽니다 (`npm run levelcurve -- 30 3 0` = 30분, 시드 3개, 난이도 0). 생존 시간을 재는 것이 아니라 경험치가 쌓이는 속도를 보는 것이 목적이라, 생존 실력이 결과에 섞이지 않도록 매 프레임 체력을 되돌립니다. **`SKILL_MAX_LEVEL` 이나 `LEVEL.skillLevels` 를 만지면 반드시 이걸 돌리십시오.** 상한만 올리고 선택 횟수를 안 늘리면 위쪽 레벨이 존재하지 않는 것과 같아지는데, 그 사실이 이 출력에만 드러납니다.

| 시점 | 첫 판(상점 없음) | 상점 전부 구매 |
|---|---|---|
| 겁쟁이·스폰율 조정 전 | 3분 46초 | 7분 13초 |
| 겁쟁이·스폰율 조정 후 | 1분 12초 | 5분 45초 |
| 레벨업 2스탯 · 원거리 탄속 반영 | 1분 16초 | 7분 34초 |
| 자폭적 재설계 · 정예 조정 · 겁쟁이 인내 | 1분 21초 | 8분 28초 |
| 회전 궤도 상시 · 돌진적 무제한 인식 · 자폭 점화 복구 | 1분 38초 | 8분 53초 |
| 정예 10종 고유 능력 · 보스 체력 2배 | 1분 46초 | 8분 32초 |
| 영구 강화 20단계 | 1분 10초 | 11분 59초 이상 |
| 스킬 7레벨 · 3레벨마다 선택 | 4분 44초 | 14분 59초 (6판 중 3판 생존) |
| ↑ 여기까지 **옛 봇** · 아래부터 **고친 봇** (비교 불가) | | |
| 스킬 7레벨 · 3레벨마다 선택 | 7분 34초 | 29분 59초 (6판 중 5판 30분 완주) |
| 스킬 **10레벨** · 시간 강화 선형화 (현재) | **8분 02초** | **29분 59초 (6판 전부 30분 완주)** |

**중간에 그은 선 위아래는 견줄 수 없습니다.** 2026-08-12 에 봇을 고쳤기 때문입니다 (아래 "봇을 고친 것" 참고). 같은 게임을 같은 봇으로 잰 것이 아니라 도구가 바뀐 것이므로, 옛 줄과 새 줄의 차이를 게임이 쉬워진 양으로 읽으면 안 됩니다.

**"영구 강화 20단계" 줄도 그 앞줄들과 조건이 다릅니다.** 봇은 살 수 있는 단계를 전부 사는데 그 상한이 3~5 에서 20 으로 늘었기 때문입니다. 앞줄과 견주려면 `PERM_MAX_LEVEL` 을 5 로 두고 다시 재야 합니다.

### 봇을 고친 것 (2026-08-12)

**봇이 사람보다 2~3배 약하게 나오고 있었고, 그 탓에 잘못된 결론을 냈습니다.** "난이도 8은 30분 클리어가 불가능해서 해금 사슬이 끊긴다"고 판단했는데, 봇을 고치니 중앙 21분 · 최대 25분이었습니다. 네 가지 중 셋을 고쳤습니다.

1. **빌드를 못 몰았습니다 (제일 큼).** `choices[0]` 을 그냥 집었는데 선택지는 위치를 못 읽게 섞여 나오므로 **사실상 무작위 빌드**였습니다. 판당 19번을 네 스킬에 고르게 흩뿌려 전부 4~5레벨에 머뭅니다. 사람은 두 개에 몰아 만렙을 만듭니다. **스킬 상한을 7 로 올린 이번 조정의 이득이 통째로 "몰아 키우기"에 있는데 봇만 그걸 못 했습니다.** `pickChoice` 로 고쳤습니다 (빈 칸부터 채우고, 다 차면 이미 가장 높은 것을 올림)
2. **예고를 아예 안 봤습니다.** `w.telegraphs` 와 `w.pendingBlasts` 가 위험 계산에 없어서 돌진 통로·시체 폭발·폭격기 예고 한가운데 서 있었습니다
3. **장판은 들어간 뒤에야 반응했습니다.** `d < 0` 일 때만 셌습니다. 지금은 가장자리부터 꺼립니다
4. **대시를 쿨마다 흘려보냅니다 (안 고쳤습니다).** 무적을 위기에 아껴 쓰는 것이 대시의 전부인데 봇은 쿨이 돌면 그냥 씁니다. 흉내 내려면 봇을 크게 고쳐야 하고, 그러다 보면 봇을 만드는 일이 게임을 만드는 일을 밀어냅니다. **봇이 사람보다 약한 이유가 아직 여기 남아 있다는 것만 기억하십시오**

고친 뒤 값입니다 (6시드, 최대 30분). 괄호 안이 **후반 곡선을 편 뒤**(열 번째 조정) 다시 잰 값입니다.

| 난이도 | 상점 없음 | 절반 구매 | 전부 구매 |
|---|---|---|---|
| 0 | 7분 34초 (8분 02초) | (29분 59초 · 3/6 완주) | 29분 59초 5/6 (**6/6 완주**) |
| 8 | 0분 24초 (0분 24초) | 7분 48초 (8분 15초) | 21분 16초 (**25분 26초 · 1/6 완주**) |
| 9 | 0분 11초 (0분 11초) | 5분 05초 (4분 00초) | 9분 58초 (**11분 10초**) |

**곡선을 펴면서 후반이 전체적으로 10~20% 쉬워졌습니다.** 30분 적 체력이 x10 → x7 이 됐으니 당연한 결과이고, 이건 곡선을 펴는 것과 맞바꾼 것입니다. 같은 난도를 유지하면서 직선으로 만들려면 `hpPerMinute` 를 0.2 → **0.3** 으로 올려야 하는데(30분 x10), 그러면 **15분 시점이 x5.5 로 예전 x4 보다 매워집니다.** 직선은 끝점을 맞추면 초반이 세지고, 초반을 맞추면 끝점이 약해집니다. 둘 다 가질 수는 없습니다.

"절반 구매"는 영구 강화 각 항목 10단계 · 가중치 상한의 절반 · 부활 1회 · 시작 스킬 1개입니다 (`saveFor`).

여기서 새로 읽히는 것 셋입니다.

- **상점 없이 난이도 8·9 는 20초 즉사입니다.** Lv.2 에서 죽어 스킬을 한 장도 못 고릅니다. 다만 정상 경로에서는 안 생깁니다 (8을 열려면 7을 깨야 하고 그때면 상점이 차 있습니다). `?unlock` 으로 건너뛴 경우에만 만나는 상황이라 급하지 않습니다
- **난이도 8 이상은 빌드가 운에 크게 좌우됩니다.** 난이도 8부터 선택지가 3장 → 2장(`skillChoiceAdd: -1`)인데, 난이도 9 절반 구매의 시드별 편차가 **1분 52초 ~ 21분 33초로 11배**입니다. 키우려는 스킬의 업그레이드 카드가 2장 안에 안 뜨면 못 올립니다. **선택 횟수를 19회로 늘려놓고 장수를 줄인 것이라, 이 항목은 이번 조정과 정면으로 부딪힙니다**
- **코인이 폭증했습니다.** 난이도 8 전부 구매가 판당 **2,861 코인**입니다 (옛 봇으로는 454). 영구 강화 전부(172,295 코인)가 60판이면 끝납니다

레벨업에 스탯이 2개씩 오르면서 상점을 산 판이 5분 45초에서 7분 34초로 늘었습니다. 첫 판은 레벨이 낮아 거의 그대로입니다. 성장의 이득이 후반에 몰린다는 뜻이라, 초반이 너무 맵다고 느껴지면 이 지점부터 보십시오.

자폭적 재설계 뒤 상점 판이 다시 7분 34초에서 8분 28초로 늘었습니다. 그때 봇은 시체 폭발 예고를 못 피했는데(지금은 봅니다) 그런데도 생존이 늘었다는 것은, "맞으면 무조건 점화"가 사라진 이득이 시체 폭발의 손해보다 컸다는 뜻입니다. 자폭적이 너무 순해졌다고 느껴지면 `triggerRange` 나 `fuse` 부터 줄이십시오.

초반이 과하다고 느껴지면 `SPAWN.rateStart`(1.3)를 먼저 낮추고, 그래도 매우면 겁쟁이 해금 조건 `ENEMY_TABLE.coward.unlockSkills`(1 → 2)을 늦추십시오. 이 둘이 초반 난도를 좌우합니다.

재미는 이걸로 검증되지 않습니다. 반드시 직접 플레이해서 확인하십시오.

## 다음에 할 일

### 열여덟 번째 조정 (2026-09-03, 모바일)

**폰에서도 됩니다. 가로로 들어야 합니다.**

| 자리 | 무엇 |
|---|---|
| 좌측 아래 | 이동 조이스틱 |
| 우측 아래 | Q (유틸 스킬) |
| 우측 위 | 좌우 정보 패널 펴기 / 접기 |

**폰에서는 좌우 패널을 접고 시작합니다.** 이게 이번 작업의 핵심입니다. 패널을 빼면 경기장만 남아 **1280 x 720, 즉 16:9** 가 되는데 폰 가로 화면과 거의 같은 비율이라 **검은 여백이 사라지고 경기장이 훨씬 커집니다.** 전체화면으로는 못 없앴던 그 여백이 패널을 접으니 없어집니다.

- **그리는 코드는 아무것도 안 바뀝니다** (`renderer.setPanelsVisible`). HUD 는 지금처럼 패널 자리에 그리고, 렌더러가 **보이는 창만 경기장으로 좁힙니다.** 안 보이는 것은 캔버스 밖으로 잘립니다. `scene.ts` 와 `hud.ts` 에 모바일이라는 개념이 등장하지 않는 이유입니다
- **체력 · 시간 · 보스 막대는 경기장 안에 그려집니다.** 패널을 접어도 이것들은 남습니다. 접히는 것은 스탯 목록과 스킬 카드뿐입니다
- **`shiftX` 를 빠뜨린 `setTransform` 이 하나라도 있으면 그 그림만 어긋납니다.** 지금 거치는 곳은 `begin` · `end` · `clear` · `fullscreenTint` 넷입니다

**반드시 지킬 것입니다.**

- **PC 에는 아무것도 안 붙습니다** (`isTouchDevice`). 마우스만 있는 기기에 조이스틱을 띄우면 화면만 가립니다
- **터치 조작은 `#overlay` 밖(`#touch`)에 답니다.** 그 안에 넣으면 화면을 바꿀 때마다 `clearOverlay()` 가 같이 지웁니다. 알림(`#toasts`)을 밖에 둔 것과 같은 이유입니다
- **판이 도는 동안에만 보입니다.** 메뉴 위에 겹치면 카드를 가립니다. 숨길 때 잡고 있던 조이스틱과 Q 를 같이 풀어야 합니다. 안 풀면 그 방향으로 굳은 채 다음 판이 시작됩니다
- **조이스틱은 키보드와 더하지 않고 갈아끼웁니다** (`Input.touch`). 더하면 둘을 같이 쥐었을 때 속도가 두 배가 되는 자리가 생깁니다
- **놓을 때는 0 이 아니라 `null` 입니다.** 0 을 넣어두면 "가만히 잡고 있는 것"과 구분이 안 되어 그 뒤로 키보드가 영영 안 먹습니다
- **조이스틱은 누른 자리가 중심입니다.** 고정된 원 한가운데를 정확히 짚게 하면 화면을 안 보고 조작할 수가 없습니다
- **Q 는 `pointerdown` 에 발동합니다.** `click` 은 손을 떼야 오는데, 유틸은 대시처럼 위기에 쓰는 것이라 그 사이가 그대로 손해입니다
- 세로로 들면 안내만 띄웁니다 (`.rotate-hint`). **게임을 멈추지는 않습니다.** 잠깐 세로가 되는 일이 있는데 그때마다 판이 끊기면 그게 더 나쁩니다
- 규칙은 `smoke.ts` 16번이 잡습니다 (조이스틱이 키보드를 갈아끼움 · 놓으면 키보드로 돌아옴 · Q 버튼이 실제 키와 같은 길로 들어감 · `clear` 가 조이스틱도 놓음)

**⚠ 브라우저 전역이 있다고 가정하지 마십시오.** `isTouchDevice` 가 `navigator` 를 그대로 읽었는데, 그 전역은 **Node 20 에 없고 21 부터 생겼습니다.** 로컬(24)에서는 통과하고 **CI(20)에서만 터졌습니다.** 배포 관문이 잡아서 나가지는 않았습니다. `typeof` 로 감싸서 없는 환경도 견디게 했습니다. 그리고 **`navigator` 는 boot 의 흉내 전역에 못 넣습니다.** Node 24 에서는 읽기 전용이라 `Object.assign` 이 터집니다.

**아직 실제 기기로 확인하지 않았습니다.** 특히 조이스틱 반경(56px)과 버튼 크기가 엄지에 맞는지는 직접 잡아봐야 압니다.

### 열일곱 번째 조정 (2026-09-03, 업적 이름)

사용자가 열 개를 짚어 바꿨습니다. **id 는 하나도 안 바꿨습니다.** 조건이 그대로이므로 id 를 바꾸면 이미 딴 사람의 달성 기록만 사라집니다 (열다섯 번째 조정에서 `level30` → `level70` 을 바꾼 것은 **조건 자체가 달라져서**였습니다).

| id | 전 | 후 |
|---|---|---|
| `playtime` | 시간 도둑 | **이 시간에 공부를 했으면** |
| `rich` | 부자 | **부자 → 집주인 → 땅주인** (단계별) |
| `shield-intact` | 방패는 구시대의 산물이다 | **캡틴 처치법** |
| `tank-alive` | 제발 죽어줘 | **게임 잘못만들었네** |
| `outrun` | 뒤지게 빠르네 | **이제 무적이지?** |
| `plant` | 너는 식물이다 | **나는 식물로 환생할테야** |
| `corpse-tool` | 도구로 쓴다 | **크리퍼는 최고야** |
| `blink-kill` | 찰나 | **아둔 토리다스** |
| `untouched` | 스치지도 않았다 | **불가능** |
| `perfect-hunt` | 완벽한 사냥 | **에이스 따운** |

**단계마다 이름이 다른 업적이 생겼습니다** (`AchieveTier.name`).

- **이름을 읽는 곳은 전부 `tierName(def, i)` 를 거쳐야 합니다.** 한 곳이라도 `def.name` 을 그대로 쓰면 화면과 알림이 서로 다른 이름을 말합니다. 지금 거치는 곳은 셋입니다 (업적 화면 · 알림 · 알림 목록)
- **업적 화면은 "지금 노리는 단계"의 이름을 씁니다.** 다 채웠으면 마지막 단계 이름으로 남습니다. 그래야 이름 자체가 다음 목표가 됩니다
- **이름을 안 준 단계는 업적 이름을 그대로 씁니다.** 나머지 단계형 업적은 손댈 필요가 없습니다

**`rich` 는 단계가 둘에서 셋이 됐습니다.** 이름이 셋이라 단계도 셋이어야 합니다. 새 단계는 누적 30,000 코인 · 보상 500 이고, **업적 보상 총액이 16,680 → 17,180 으로 늘었습니다.**

### 열여섯 번째 조정 (2026-09-03, 검증 구멍 메우기)

밸런스와 무관하게 **점검이 못 잡고 있던 자리** 넷을 고쳤습니다.

#### (1) 부팅 점검이 게임오버에 확실히 닿게

`npm run check` 의 부팅 점검 6번(게임오버)이 **판마다 되다 말다 했습니다.** 게임 자체는
멀쩡했고 점검 방식이 문제였습니다.

예전 방식은 P키로 200마리를 20묶음마다 붓고 플레이어가 죽기를 기다리는 것이었습니다.
**그런데 그 처치로 들어오는 경험치가 플레이어를 같이 키웁니다.** 같은 스크립트를 반복
실행한 계측입니다.

| 판 | 결과 | 도달 레벨 | 처치 | 버틴 시간 |
|---|---|---|---|---|
| A | 사망 | Lv.34 | 4,000 | 42.9초 |
| B | 사망 | Lv.29 | 2,562 | 34.6초 |
| C | **생존** | Lv.45 | 8,286 | 70초(예산 소진) |

**압박을 키우려고 더 부으면 오히려 반대로 갑니다.** 열 번째 조정(스킬 10레벨 · 곡선 펴기)
뒤로 C 쪽이 자주 나오면서 연달아 실패하고 있었습니다.

**고친 것은 둘입니다.**

- **디버그 즉사 키 X 를 만들었습니다** (`game/debug.ts`). 가장 가까운 적을 사인으로 달아
  `damagePlayer` 로 죽이므로 **사망 원인 표시까지 같이 검증**됩니다. **부활은 그대로
  태웁니다.** 남아 있으면 한 번 되살아나므로 판을 끝내려면 남은 횟수만큼 더 눌러야
  합니다. 여기서 부활을 지우면 그 경로를 영영 못 밟습니다. 무적(G)을 켜둔 채 누르면
  죽은 뒤에 체력만 되돌아오므로 **누르는 순간 무적을 끕니다**
- **부팅 점검 6번이 그 키를 씁니다** (`scripts/boot.ts`). 적을 한 번만 부르는 것은 사망
  원인 칸을 띄우기 위해서이고, 그다음은 X 로 곧장 끝냅니다. 5회 연속 통과를 확인했습니다

**반드시 지킬 것입니다.**

- **싸움의 승패에 점검을 매달지 마십시오.** 밸런스를 만질 때마다 점검이 뒤집히면, 그때마다
  통과할 때까지 조건을 손보게 됩니다. 그건 점검이 아닙니다
- **판이 아직 재현되지 않습니다.** `?seed` 가 없으면 `Rng` 가 `Math.random()` 으로 씨앗을
  뽑는데(`core/rng.ts`), `boot.ts` 는 그것을 안 넘깁니다. 6번은 이제 싸움과 무관하지만
  다른 항목이 흔들리면 여기부터 보십시오

#### (2) 저장에 없는 스킬 id 가 있으면 게임이 안 켜지던 것

`save.ts` 가 시작 스킬 목록을 **배열인지만 보고 통과**시키고 있었습니다. 바로 옆의
`equippedPassives` 는 해금 목록과 대조하는데 이 셋만 그냥 지나갔습니다.

없는 id 를 하나 넣고 돌려보니 이렇습니다.

```
상점에서 터짐 → Cannot read properties of undefined (reading 'kind')
판 시작에서 터짐 → Cannot read properties of undefined (reading 'kind')
```

`getSkillDef` 는 표에 없는 id 에 `undefined` 를 돌려주는데 받는 쪽이 곧바로 `.kind` 를
읽습니다. **터지는 자리가 판 시작(`player.ts`)과 상점(`shop.ts`) 둘 다라, 게임을 켤 수도
없고 고칠 화면에 들어갈 수도 없습니다.** 저장을 통째로 지우는 것 말고 길이 없었습니다.

`knownSkills` 하나로 셋을 다 거릅니다. **`records.bestBuild` 도 같은 목록입니다.**
기록 화면(`menus.ts`)이 그걸로 스킬 이름을 뽑기 때문에 안 거르면 거기서 같이 터집니다.

- **거르는 기준은 `balance.ts` 의 `SKILLS` 입니다.** `save.ts` 가 스킬 등록부를 통째로
  끌어오지 않게 하려는 것이고, 대신 **그 표와 등록부가 같은 목록인지를 `smoke.ts` 16번이
  잽니다.** 두 곳이 갈라지면 거르는 기준 자체가 틀어지므로 이 시험을 지우지 마십시오
- **스킬 id 를 바꾸거나 없앨 때 마이그레이션을 따로 쓸 필요는 없습니다.** 모르는 이름은
  조용히 빠지고 그 자리는 안 산 것이 됩니다. 코인을 돌려주고 싶으면 그때 따로 넣으십시오

#### (3) 투사체 감속이 `slowEnemy` 를 안 거치던 것

`projectile.ts` 가 `e.slow` 를 직접 건드리고 있었습니다. 그 길로는 보스 상태이상
저항(70%)도, 돌진 중 상태이상 면역(`statusImmune`)도 안 걸립니다.

**지금까지 아무 일도 없었던 이유는 `slowFactor` 를 1 미만으로 넣는 스킬이 하나도 없어서**
그 if 문이 한 번도 참이 된 적이 없었기 때문입니다. 감속 투사체를 하나 붙이는 순간 두
규칙이 소리 없이 빠지는 자리였습니다. 지금은 `w.slowEnemy` 를 부릅니다.

#### (4) 검증 스크립트가 타입 검사 밖에 있던 것

`tsconfig.json` 의 `include` 가 `["src"]` 뿐이라 **`npm run check` 의 타입 검사가
`scripts/` 를 아예 안 봤습니다.** 게임의 검증을 그 폴더가 전부 맡고 있는데 정작 그 폴더가
무검사였습니다. `["src", "scripts"]` 로 바꾸자 오류 넷이 나왔습니다.

| 파일 | 무엇이었나 |
|---|---|
| `boot.ts` | `DIFFICULTY.unlockTime` 을 읽는데 **그런 상수가 없습니다.** 다섯 번째 조정에서 해금 규칙이 바뀌며 사라진 값이라, `bestTime` 에 `undefined` 를 넣고 있었습니다 (JSON 이 그 칸을 버려서 겉으로는 멀쩡했습니다). 줄째로 지웠습니다 |
| `boot.ts` | DOM 흉내 객체에 `textContent` 가 빠져 있었습니다 |
| `bot.ts` | 시작 스킬 목록이 `string[]` 로 새고 있었습니다. **`satisfies SkillId[]` 로 바꿨습니다.** `as` 로 덮으면 없는 이름을 적어도 그냥 통과합니다 |
| `smoke.ts` | `generateSkillChoices` 중복 import 와 안 쓰는 `branchMods` |

**앞으로 `scripts/` 도 타입 검사를 받습니다.** 검증 도구를 고칠 때 오류가 나면 그건 도구가
틀린 것이니 무시하지 마십시오.

### 열다섯 번째 조정 (2026-08-16, 업적 조건 손질)

적을 재설계하면서 조건의 뜻이 달라졌거나 애초에 재는 방식이 틀렸던 일곱 개를 고쳤습니다.

| 업적 | 전 | 후 |
|---|---|---|
| 방패는 구시대의 산물이다 | 방패 남긴 채 처치 | **난이도 6 이상** 조건 추가 |
| 제발 죽어줘 | 탱커 3분 생존 | **난이도 9 이상** 조건 추가 |
| 뒤지게 빠르네 | 시간·난이도 배율이 붙은 속도 기준 | **표에 적힌 기본 이속** 기준 |
| 도구로 쓴다 | 시체 폭발 5마리 | **분열체는 안 셉니다** |
| 찰나 | 드러나 있는 동안 처치 | 드러난 뒤 **0.1초** 안에 처치 |
| 폭주 | 레벨 30 | 레벨 **70** |
| 마우스 조작은 안해요 | 판 중 클릭 | **스킬 선택 클릭은 제외** |

**반드시 지킬 것입니다.**

- **판마다 달라지는 기준은 업적이 될 수 없습니다.** "뒤지게 빠르네" 가 `w.timeScale().speed`
  를 곱하고 있어서 기준선이 판이 갈수록 달아났습니다. 같은 이동속도로도 1분에는 열리고
  20분에는 안 열리는 조건이었습니다. 지금은 `fastestEnemySpeed()` 가 인자를 안 받습니다
- **"도구로 쓴다" 는 `World.blastKills` 로 셉니다.** 예전에는 `stats.kills` 의 차이로 쟀는데,
  분열적 하나를 터뜨리면 그 자리에서 3마리가 더 죽어 마릿수가 저절로 불어났습니다.
  그러면 "잡몹 한가운데서 터뜨렸다"가 아니라 "분열적 옆에서 터뜨렸다"가 됩니다.
  연쇄 자체는 여전히 성과로 봅니다. **빠지는 것은 `e.child` 뿐입니다**
- **"찰나" 는 은신적이 드러난 시각을 `state.timer` 에서 읽습니다.** 은신이 거리 기준으로
  바뀌면서(열네 번째 조정) 붙어 있으면 계속 드러난 상태라, 예전 조건은 그냥 "가까이서
  잡았다" 가 되어 버렸습니다. 드러나는 순간을 적어두는 곳은 `advanced.ts` 의 `stealth` 입니다
- **"폭주" 의 id 를 `level30` → `level70` 으로 바꿨습니다.** 조건이 통째로 달라졌는데 id 를
  그대로 두면 예전에 레벨 30 으로 딴 사람이 70 에 닿은 적도 없이 달성 상태로 남습니다.
  저장에 남은 옛 키는 목록에 없는 값이라 무시되고, 진행도 분모는 표에서 세므로 영향이 없습니다
- **난이도 조건이 붙은 둘은 `c.w.difficulty` 를 봅니다.** `maxDifficulty`(어디까지 열렸는가)가
  아니라 지금 이 판의 난이도입니다

### 열네 번째 조정 (2026-08-16, 적 재설계 · 예고 통로 · 코인)

사용자가 플레이하며 짚은 것 일곱 개입니다.

**1. 돌진 예고 통로가 실제로 멈추는 지점까지만 그어집니다** (`advanced.ts` 의 `wallDistance`)

**막대가 다 찬 것처럼 보이는데 돌진을 안 하는** 문제의 원인입니다. 통로를 화면 대각선
길이(약 1469px)만큼 그어놓고 차오르는 앞머리를 그 전체 길이에 걸쳐 3초 동안 움직였는데,
**눈에 보이는 것은 벽까지 잘린 부분뿐**입니다. 벽이 400px 앞이면 0.75초 만에 보이는
통로가 꽉 차 보이고 나머지 2.25초는 그대로 서 있었습니다.

- **"다 차면 튀어나온다"는 약속이 이 화면의 전부입니다.** 통로 길이를 바꾸는 순간
  그 약속이 깨지므로, 끝점을 계산하는 곳은 `wallDistance` 하나뿐이어야 합니다
- 돌진은 벽에서 `radius + 2` 만큼 떨어진 지점에서 멈추므로 통로도 거기서 끝냅니다
- `scripts/smoke.ts` 가 **통로가 경기장을 벗어나지 않는지** 직접 잽니다

**2. 돌진하는 동안은 상태이상에 면역입니다** (`Enemy.statusImmune`)

3초를 예고하고 튀어나가는 한 번을 기절이나 감속으로 지울 수 있으면, **예고를 보고
자리를 비킨다는 대응 자체가 필요 없어집니다.** 돌진이 끝나면 곧바로 풀립니다.

- **막는 것은 기절 · 감속 · 넉백입니다. 화상의 지속 피해는 그대로 들어갑니다.**
  보스 상태이상 저항에서 화상을 뺀 것과 같은 이유입니다. 화상은 상태이상이라기보다
  지속 피해라, 여기 넣으면 "돌진적에게는 화력이 안 통한다"가 됩니다.
  다만 **화상이 주는 10% 감속은 면역에 포함**됩니다 (`update.ts` 의 `slowAmount`)
- 돌진에 들어가는 순간 이미 걸려 있던 것도 걷어냅니다. 안 걷으면 예고 중에 건 감속이
  돌진 속도를 그대로 깎습니다

**3. 코인이 벽에서 튕깁니다** (`COIN.wallBounce`, `coin.ts` 의 `bounceOffWalls`)

떨어질 때 무작위 방향으로 튀는데, 가장자리에서 죽은 적의 코인이 경기장 밖으로 나가
영영 못 줍는 자리에 남았습니다.

**4. 은신적은 시간이 아니라 거리로 드러납니다** (`stealth.revealRange` 230)

예전에는 5초마다 1초씩 켜졌다 꺼졌는데, 그러면 멀리 있는 적이 혼자 깜빡이기만 하고
정작 붙었을 때는 안 보이는 순간이 생겼습니다. 접촉 피해가 가장 큰 적이라는 성격과도
어긋났습니다. **정예는 절반 거리(115)까지 붙어야 보입니다.**

- **드러난 동안에만 타겟이 된다는 규칙은 그대로입니다** (아래 "반드시 지킬 것" 2번)

**5. 유령적이 미라적이 됐습니다** (`mummy`)

유령의 "다른 적을 통과한다"는 화면에서 거의 읽히지 않았고 컨셉도 겹쳤습니다. 통째로
바꿨습니다. 근접이고 체력은 탱커(3.0) 바로 아래인 2.6 입니다.

| | 값 |
|---|---|
| 첫 죽음 | 죽지 않고 그 자리에 **3초간 쓰러집니다** (정예 1.5초) |
| 쓰러진 동안 | 행동 · 접촉 피해 · 피격 · 분리 전부 없음. 흐리게 보이고 조여드는 링으로 남은 시간 표시 |
| 부활 직후 | 체력 **x3** · 공격력 **x2** · 이동속도 **x1.5** |
| 그 뒤 | 속도는 매초 10%p 씩 깎여 5초 뒤 원래대로. 체력은 **매초 최대치의 20%** 씩 소멸 |
| 두 번째 죽음 | 진짜 죽음 (`e.revived` 로 갈림) |

- **`onLethal` 은 `damageEnemy` 에서만 불립니다.** 파편이나 보스의 포식처럼 `killEnemy`
  를 직접 부르는 길은 그대로 진짜 죽음입니다. 죽는 연출 중에 미라가 되살아나면
  "시간이 멈춘 자리"라는 그 연출의 전제가 깨집니다
- **부활 속도의 기준은 그때의 `e.speed` 입니다.** 표를 다시 읽어 계산하면 시간 강화와
  난이도 배율이 통째로 빠집니다
- **쓰러진 동안 피해가 안 들어가야 합니다** (`damageEnemy` 첫 줄). 안 막으면 장판이나
  화상이 시체를 계속 때려서 부활 전에 두 번째 죽음이 옵니다
- 가만히 둬도 부활 후 5초면 스스로 무너지므로 **"지금 정리할까 버틸까"의 판단**이 됩니다

**6. 소환적은 못 죽이는 하수인을 부릅니다** (`summoner.minionPool`)

| | 값 |
|---|---|
| 부르는 것 | **무적 하수인**. 피해도 안 받고 타겟도 안 됩니다 |
| 종류 | 바보 · 돌진 · 탱커 중 하나. **스폰할 때 정해지고 안 바뀝니다** |
| 상한 | 한 마리당 동시 5마리 |
| 사라지는 법 | **본체를 잡는 것뿐.** 잡는 순간 전부 한꺼번에 소멸 |
| 보상 | 소멸한 하수인마다 25% 확률로 코인 1개 |
| 정예 | 부르는 하수인이 정예가 됩니다. 그 외는 일반과 동일 |

- **하수인 소멸은 `killEnemy` 를 타지 않습니다** (`World.despawnMinions`). 그쪽을 거치면
  경험치 · 처치 통계 · 업적이 하수인 수만큼 붙어서 소환적 하나가 여섯 마리 몫의
  보상을 줍니다. 보상은 확률로 떨어지는 코인뿐입니다
- **종류를 고정한 이유**는 매번 다른 것이 나오면 무엇을 상대하는지 알 수 없어서입니다.
  고정이면 화면을 보고 대응을 정할 수 있습니다
- **정예 소환적의 순간이동 능력은 없앴습니다.** 하수인이 본체를 잡아야만 사라지는데
  본체가 반대편으로 계속 달아나면 잡을 방법이 사실상 없어집니다
- **하수인은 화면의 적 상한에 안 셉니다** (`World.countedAlive`). 본체를 잡아야만 사라지므로
  상한에 넣으면 소환적 넷이 `SPAWN.maxAlive` 의 4분의 1을 판이 끝날 때까지 붙들고 있어,
  후반에 다른 적이 아예 안 나오는 판이 됩니다. 하수인은 스폰 압력이 아니라 소환적이
  걸어둔 자물쇠라 성질이 다릅니다. **상한을 보는 곳은 전부 이 함수를 거쳐야 합니다.**
  한 곳이라도 `enemies.length` 를 그대로 쓰면 그쪽에서만 세어져서 규칙이 갈립니다
- 종류별 상한(`ENEMY_TABLE[id].maxAlive`)에서도 뺍니다. 안 빼면 소환적이 부른 탱커 때문에
  진짜 탱커가 안 나오는 식으로, 소환적이 다른 적의 등장을 막게 됩니다

**7. 장판 지속시간 10초 → 5초** (`puddle.hazardDuration`). 정예에도 지속 배율이 따로
없으므로 이 한 값이 둘 다 덮습니다. 난이도의 `hazardDurationMul` 은 여기에 곱해집니다.

**아직 플레이로 확인하지 않았습니다.** 특히 미라의 부활 수치와 무적 하수인의 압박이
어느 정도인지는 직접 해봐야 압니다.

### 열세 번째 조정 (2026-08-16, 화면 문구 걷어내기 · 유틸 레벨 유지)

**화면에서 "설명"을 전부 걷어냈습니다.** 사용자가 화면 하나씩 짚어가며 정한 것이고, 기준은 하나입니다.

> **판마다 바뀌지 않는 것은 안 적습니다. 지금 값만 적습니다.**

무엇이 설명이고 무엇이 아닌지는 이 질문으로 갈립니다. **다음 판에도 같은 글자가 뜨는가.** 같으면 설명이라 지웠고, 판마다 달라지면 상태라 남겼습니다. `장착 1 / 2` · `남은 2회` · `적 강화 x1.5` · `준비됨` 이 남은 이유가 전부 이것입니다.

지운 것을 화면별로 적으면 이렇습니다.

| 화면 | 지운 것 |
|---|---|
| 메인 | 조작 안내 2줄 · 부제 · 카드 부제 4개 (업적의 `12 / 53` 만 남김) |
| 도감 · 업적 | 부제 · 보상 안내 |
| 난이도 | 누적 설명 · 해금 규칙 설명 (부제와 잠김 문구가 이미 두 번 말합니다) |
| 상점 | 탭 꼬리말 2개 · 패시브 칸 설명 3종 · 봉인 설명 · 시작 스킬 설명 · 계열 설명 |
| 레벨업 | 부제 · 스킬 설명 · 타겟팅 · 주 상대 · `자동 발동` · 리롤/건너뛰기 설명 |
| 갈래 | 부제 · 24갈래 설명 |
| 게임 중 HUD | 카드의 `자동` · `직접 사용` · `타겟` + 타겟팅 · 기본공격의 `가장 가까운 적` |

**대신 접어 넣을 자리를 세 곳 만들었습니다.** 지우기만 하면 처음 켠 사람이 아무것도 알 수 없으므로, "늘 보이는 글"을 "찾으면 있는 글"로 옮긴 것입니다.

1. **메인 좌측 상단 `조작법`** (`dom.ts` 의 `helpButton`). 마우스를 올리면 키 목록이 펴집니다. **키만 답니다.** 여기에 규칙 설명을 적기 시작하면 접어둔 의미가 없어집니다
2. **상점 탭 줄 오른쪽 `?`** (`dom.ts` 의 `helpDot`, 내용은 `shop.ts` 의 `tabHelp`). 지금 보고 있는 탭이 무엇을 파는지 보여줍니다
3. **상점 첫 방문 대화상자** (`shop.ts` 의 `introDialog`, `SaveData.shopHelpSeen`). 탭 넷을 한 번에 폅니다. 닫으면 저장에 남아 다시 안 뜹니다

**반드시 지킬 것입니다.**

- **화면 판별은 문구가 아니라 표식(class)으로 합니다.** `boot.ts` 가 `'스킬 하나를 고릅니다'` 라는 부제로 스킬 창과 갈래 창을 갈랐는데, 그 문구를 지우자 **두 화면이 구분되지 않았습니다.** 지금은 `screen()` 의 `extraClass` 에 붙인 `skill-choice` · `branch-choice` 를 `querySelector` 로 봅니다. 앞으로 문구를 아무리 고쳐도 시험이 안 깨집니다
- **수치는 설명이 아닙니다.** 상점 스탯 강화의 `시작 최대 체력 +15` 는 산문처럼 보이지만, 그 120 코인이 얼마를 올리는지가 **화면에서 여기 하나뿐**이라 남겼습니다. 지우려면 다른 자리에 그 숫자가 먼저 있어야 합니다
- **상점 첫 방문 도움말에서 Esc 는 도움말을 먼저 닫습니다.** 안 그러면 처음 들어온 사람이 Esc 한 번에 상점 밖으로 튕겨 나가면서 도움말도 읽은 것으로 처리됩니다. 설정 화면의 "겨눔 해제"와 같은 규칙이고, `boot.ts` 가 순서를 잡습니다
- **도움말은 상점 안이 아니라 상점 위에 뜹니다** (`.modal-back`). 목록에 끼워 넣으면 탭과 카드가 아래로 밀려서, 처음 본 사람에게는 그것이 상점의 원래 모습으로 보입니다. `#overlay` 가 grid 라 `position: absolute` 로 띄워야 아래 줄에 안 쌓입니다
- **`타겟팅 규칙`은 F1 오버레이에만 남았습니다** (`debug.ts`). `def.targetingLabel` 을 지우지 않은 이유가 이것입니다. 스킬마다 타겟팅이 다르다는 규칙(아래 "반드시 지킬 것" 7번)은 그대로이고, 확인할 자리만 옮겼습니다
- **HUD 스킬 카드 높이는 `CARD_H`(92) 한 곳입니다.** 타겟팅 설명을 빼면서 134 → 92 가 됐습니다. 카드 안의 좌표를 만질 때 숫자를 여기저기 적지 마십시오
- **부활 체력 비율은 `REVIVE_UPGRADE.hpRatio` 한 곳입니다.** `world.resolveDown` 과 상점 문구가 각각 `0.55` 와 `55%` 를 따로 적고 있었습니다. 한쪽만 고치면 **화면이 거짓말을 합니다**

**유틸 스킬은 교체해도 레벨이 유지됩니다** (`skillChoice.ts`). 이건 문구가 아니라 게임 규칙 변경입니다.

예전에는 1레벨로 되돌아갔는데, 그러면 판 중반 이후의 유틸 교체가 **사실상 금지**였습니다. 5레벨 대시를 버리고 1레벨 시간 감속으로 갈아탈 사람은 없으니, "유틸 1칸"이라는 제약이 실제로는 "처음 뽑은 것을 판 끝까지 쓴다"로 굳어져 있었습니다. 이제 교체가 손해가 아니라 **갈아타기**입니다. 선택지 카드도 `시간 감속 Lv.5 [교체]` 로 뜨고 오른쪽 수치도 5레벨 기준입니다.

- **선택지를 만드는 쪽과 적용하는 쪽이 같은 레벨을 봐야 합니다.** `generateSkillChoices` 가 `level: p.utility?.level ?? 1` 을 실어 보내고 `applySkillChoice` 가 그것을 그대로 씁니다. 한쪽만 고치면 **카드에 적힌 수치와 고른 뒤의 성능이 갈립니다.** `scripts/smoke.ts` 4-8b 가 둘 다 잡습니다

**아직 플레이로 확인하지 않았습니다.** 특히 HUD 에서 타겟팅을 뺀 것이 실제로 답답한지는 직접 해봐야 압니다.

### 열두 번째 조정 (2026-08-13, 공격 스킬 6레벨 강화 갈래)

**공격 스킬 12종이 스킬 레벨 6에 닿으면 두 갈래 중 하나를 고릅니다.** 레벨업 선택창이 닫힌 뒤 전용 창이 이어서 뜹니다. 표는 `SKILL_BRANCHES`(`balance.ts`)에 있습니다.

그전까지 스킬 성장은 **숫자만 커지는 한 줄기**였습니다. 10레벨 지뢰는 1레벨 지뢰가 그냥 세진 것이라, 스킬을 고른 뒤에는 판이 끝날 때까지 아무 결정도 없었습니다. 판당 갈래 선택은 2~4회쯤 옵니다.

**1번은 강화, 2번은 특수로 자리가 고정입니다.**

| | 1번 · 강화 | 2번 · 특수 |
|---|---|---|
| 바뀌는 것 | 수치만. 하던 일을 더 크게·세게 | **없던 동작이 붙습니다** |
| 대가 | 같은 축 안에서 맞바꿈 (개수↔위력) | 기본 수치가 내려갑니다 |
| 구현 | 표에 수치만 | registry 수정이 필요합니다 |

자리가 성격을 뜻하면 두 카드를 다 읽지 않아도 무엇을 고르는지 압니다. 그래서 **1번에는 덮어쓰기 항목이 하나도 없어야 하고 2번에는 하나 이상 있어야 합니다.** `scripts/smoke.ts` 3-0c 가 이걸 잡습니다.

**세기 규칙은 "서로 다른 축을 강화하되 각자 대가를 진다"입니다.** 패시브의 제1규칙(페널티 > 보너스)과 반대인데, **패시브는 무제한 보유라 스스로 제한이 걸려야 하지만 갈래는 1중 1 배타라 포기한 반대편이 이미 대가**이기 때문입니다.

| 스킬 | 1 · 강화 | 2 · 특수 |
|---|---|---|
| 산탄 | 슬러그탄 (발수 1/3 · 위력 3.5배 · 사거리 2배) | **화염 산탄** (맞은 적 화상) |
| 스나이퍼 | 철갑탄 (최대체력 비례 2배) | 처형 저격 (체력 30% 이하 피해 3배) |
| 화염방사기 | 연장 총구 (사거리 2배 · 지속 1.5배) | 가스 분출 (전방위 · 쿨 0.3배) |
| 레이저 | 집속 레이저 (위력 2.6배 · 폭 0.4배) | **작열 레이저** (관통한 적 전부 화상) |
| 유탄 | 대형 탄두 (반경 1.7배 · 위력 1.6배) | 집속탄 (자탄 4발 추가 폭발) |
| 미사일 | 증폭 탄두 (위력 1.8배 · 반경 1.6배) | 분열 미사일 (명중 시 3발로 갈라짐) |
| 체인 | 과부하 (1타 2.2배 · 마비 2.4배) | 전도 (감쇠 없음 · 연쇄 1.5배) |
| 도탄 | 강화 도탄 (위력 1.9배 · 명중 1.4배) | 분열 도탄 (맞을 때마다 갈라짐) |
| 작살 | 작살포 (폭 2.5배 · 끌어당김 1.8배) | 사슬 작살 (꿰뚫린 적 기절 1.2초) |
| 회전 궤도 | 거대 구체 (크기 2.2배 · 위력 2.4배) | 파편 궤도 (스칠 때마다 작은 폭발) |
| 지뢰 | 거대 지뢰 (개수 1/3 · 위력 3배 · 반경 2배) | **화염 지뢰** (터진 자리에 3초 화염 장판) |
| 오라 | 확장 오라 (반경 1.6배 · 위력 1.5배) | 파열 (꺼질 때 반경 전체 폭발) |

**분기는 "if" 가 아니라 "곱하는 값"입니다.** 24갈래를 `if (slot.branch === 'mineGiant')` 로 쓰면 registry 12곳이 조건문 덤불이 됩니다. 분기를 읽는 함수는 `branchMods(slot)` 하나뿐이고 **분기가 없어도 완전히 채워진 묶음**(전부 중립값)을 돌려주므로, 부르는 쪽은 갈래가 있는지 물어볼 일이 없습니다 (`src/skills/branches.ts`). 정말 매개변수화가 안 되는 것이 나와도 **hook 을 만들지 마십시오.** 하나 만들면 곧 열두 개가 됩니다.

**나중에 터지는 것은 분기를 되묻지 않습니다.** 지뢰가 터지는 시점에는 슬롯이 없습니다. 그래서 쏠 때 결과물을 투사체에 실어 보냅니다(`Projectile.blastHazard` · `execute` · `cluster` · `splitOnHit` · `orbFragment`). 덕분에 `projectile.ts` 에는 분기라는 개념이 아예 등장하지 않습니다.

**반드시 지킬 것입니다.**

- **쿨다운은 `slotCooldown(stats, slot)` 하나로만 잽니다.** 예전에는 `player.ts` 와 `hud.ts` 가 `def.cooldown` 을 따로 읽었는데, 갈래가 쿨을 바꾸는데 한 곳만 고치면 **막대가 엉뚱한 길이로 차오릅니다.** 게임은 안 멈추고 화면만 거짓말을 합니다
- **`화상 지속 ≥ 쿨다운` 부등호를 갈래 쿨다운으로도 재야 합니다.** 그래서 연장 총구에는 쿨 배율을 안 붙였습니다. 화염방사기 갈래에 쿨 증가를 붙이면 매 주기 불이 꺼져서 "재생을 멈춘다"는 정체성이 무너지는데, 표를 보고는 그 연결이 안 보입니다
- **`sizeMul` 은 궤도에서 구체 크기(`bodyRadius`)입니다. 공전 반경이 아닙니다.** 공전 반경을 건드리면 타격 간격을 `0.32 x 70/반경` 으로 같이 맞춰야 하고, 안 맞추면 **조용히 36% 하향**이 됩니다
- **거대 지뢰의 `countMul x damageMul` 은 정확히 1 입니다.** 총합을 안 바꾸고 형태만 바꾸는 갈래라, 이 곱이 1 을 벗어나면 "형태를 바꾸는 갈래"가 슬그머니 "세지는 갈래"가 된 것입니다
- **방패에 막히는 셋(산탄·화염방사기·미사일)의 어느 갈래도 `ignoreShield` 를 켜지 않습니다.** 갈래로 약점을 지우기 시작하면 6레벨 이후에는 방패적이 없는 게임이 됩니다. **화염 산탄도 방패에 막히면 화상이 안 붙습니다**(`dealt > 0` 규칙)
- **분열은 예산제입니다** (`splitsLeft`). 부모의 남은 분열 횟수를 자식들이 나눠 가지므로 한 발에서 나오는 탄이 상한을 못 넘습니다. 예산이 없으면 적이 몰린 곳에서 기하급수로 불어나 화면이 탄으로 덮입니다

**화상은 계열마다 최소 하나 있어야 합니다.** 예전에는 화염방사기 하나뿐이라 "화상 빌드"가 사실상 "화염방사기를 뽑았는가"였고, 지속 계열을 안 쓰는 빌드에는 **정예 탱커의 재생을 멈출 방법 자체가 없었습니다.** 지금은 지속=화염방사기, 폭발=화염 지뢰, 탄환=화염 산탄, 관통=작열 레이저입니다. 둘이 되어도 되고 **지키는 것은 하한뿐**입니다.

**화상은 겹쳐도 합해지지 않습니다** (`Math.max`). 감속을 합연산으로 바꾼 것과 어긋나 보이지만 성질이 다릅니다. 감속은 **양**이라 45% + 10% = 55% 가 성립하지만, 화상은 **상태**라 "타고 있다/아니다" 하나이고 재생 정지와 감속 10% 는 두 번 걸릴 수 없습니다. 합연산이면 화상 갈래들이 서로를 강제하는 조합이 되어 "무엇을 포기하는가"가 사라집니다.

**장판에 편이 생겼습니다** (`Hazard.side`). 예전에는 장판이 완전히 플레이어 전용이었습니다. 지금은 `'enemy'` 인 장판이 `updateHazards` 에서 스스로 적을 때립니다.

- **`collidePlayer` 는 `side !== 'player'` 를 건너뜁니다.** 없으면 내 화염 지뢰가 내 발밑을 태웁니다
- **난이도의 `hazardDurationMul` 은 적이 깐 장판에만 겁니다.** 내 장판까지 늘어나면 난이도가 오를수록 내 스킬이 세지는 뒤집힘이 생깁니다
- **`bot.ts` 의 `dangerAt` 도 `side` 를 봅니다.** 없으면 봇이 자기 불에서 도망다녀 생존 시간이 밸런스와 무관하게 떨어집니다
- 장판이 적을 때릴 때는 `fromX/fromY` 를 안 넘겨서 **방패 판정을 안 탑니다.** 발밑의 불을 정면 방패로 가릴 수는 없습니다

**봇은 항상 1번(강화) 갈래를 고릅니다** (`bot.ts` 의 `drainBranches`). `w.rng` 를 쓰면 그 판의 이후 난수가 통째로 밀려서 시드 고정과 예전 측정값 비교가 어긋납니다. **봇 수치가 강화 갈래에 치우친다는 것을 알고 읽으십시오.**

**갈래 큐(`World.pendingBranchChoices`)를 비우는 곳이 다섯입니다** (`main.ts`, `boot.ts`, `survival.ts`, `levelcurve.ts`, `smoke.ts`). 하나라도 빠뜨리면 큐가 무한히 쌓이고 그 측정은 "분기가 없는 게임"을 잰 값이 됩니다. **오류가 안 나므로 눈치채기 어렵습니다.**

**`skillChoice.ts` 의 push 에는 `kind === 'attack'` 검사가 반드시 있어야 합니다.** 없으면 유틸이 6레벨일 때 카드 0장짜리 화면이 뜨고, 키를 아무리 눌러도 안 닫혀 **판이 영구 정지합니다.**

**`main.ts` 의 HUD 그리기 목록에 `'branch'` 가 있어야 합니다.** 빠뜨리면 갈래를 고르는 동안 화면이 통째로 사라집니다. 드레인에 `return` 이 없으면 같은 프레임에 화면 두 개가 열립니다.

**부팅 점검에서 갈래 확인은 게임오버 뒤 새 판으로 합니다** (`boot.ts` 7번). 6레벨까지 올리려면 강제 레벨업을 수십 번 해야 하는데 그러면 플레이어가 너무 세져서 게임오버 점검이 영영 안 끝납니다. 두 점검이 서로를 방해하므로 자리를 나눴습니다. **F1 은 토글이고 그 상태가 판을 넘어 유지되므로 새 판에서 다시 누르면 꺼집니다.**

**아직 플레이로 확인하지 않았습니다.** 24갈래의 수치는 전부 초안입니다.

### 열한 번째 조정 (2026-08-13, 스나이퍼 · 화염방사기 · 감속 합연산)

**1. 스나이퍼: 쿨 3.2 → 2.5초, 위력 배수 곡선째 2배, 그리고 대상 최대 체력 비례 추가 피해**

1레벨 10% → 만렙 30% 입니다 (`SKILLS.sniper.hpRatio`). 물량 게임에서 단일 대상 스킬은 **"총 화력"이라는 같은 축으로는 산탄·체인을 절대 못 이깁니다.** 그래서 축을 바꿨습니다. 잡몹에는 거의 안 붙고(30분 난이도 15 기본적에 97, 한 발 1,437 중 7%) 탱커·정예·보스에만 크게 붙습니다(30분 난이도 15 정예 탱커에 873).

**조건을 "보스"로 잡지 않은 이유**는 보스가 살아 있는 시간이 판의 5~10% 뿐이라서입니다. 보스 한정 보너스면 공격 3칸 중 하나를 나머지 90% 동안 놀리게 됩니다. 최대 체력 기준이면 판 내내 일하면서도 대상은 여전히 단단한 적으로 좁혀집니다.

**반드시 지킬 것 셋입니다.**

- **비례항은 쏠 때가 아니라 맞는 순간 실제로 맞은 적을 기준으로 셈합니다** (`Projectile.hpBonus`, `projectile.ts` 의 `hitEnemies`). 스나이퍼는 유도가 없어서 겨눈 보스와 실제로 맞는 적이 다를 수 있습니다. 쏠 때 굳히면 앞을 가로막은 잡몹에 보스 몫이 들어갑니다
- **치명타는 비례항에 안 곱해집니다.** 치명타 배율 상한이 4.0 이라 곱하면 만렙에서 대상 최대 체력의 **120%** 가 한 발에 들어가서 무엇이든 크리 한 방에 지워집니다. 치명타는 공격력 스탯에 걸리는 배율이고 이쪽은 대상에 걸리는 항이라 성질도 다릅니다
- **남은 체력이 아니라 최대 체력입니다.** 남은 체력 기준이면 계속 반감이라 끝을 못 냅니다. 처형(남은 체력 비례)은 패시브 축 초안에 이미 잡혀 있어서 겹치기도 합니다

규칙은 `scripts/smoke.ts` 3-3번이 잡습니다.

**2. 화염방사기: 화상 지속 = 재사용 대기 (`FLAME_COOLDOWN` 5.0초)**

예전에는 화상이 3초인데 쿨이 5초라 낮은 레벨에서 매 주기 0.4초씩 불이 꺼졌습니다. 그 틈에 정예 탱커가 재생을 되찾아서 **"재생을 멈춘다"는 이 스킬의 정체성이 반쯤 헛돌았습니다.**

분사 시간(`duration`)으로 메우는 방법도 있었지만 그건 **적이 부채꼴 안에 끝까지 머물러 있을 때만** 성립합니다. 스치고 지나간 적은 여전히 꺼집니다. 쿨다운에 맞추면 틱 한 번만 닿아도 다음 분사까지 탑니다. 그래서 **쿨다운과 화상 지속이 같은 상수를 봅니다.** 하나만 고치면 부등호가 깨지므로 `FLAME_COOLDOWN` 한 곳만 만지십시오. `scripts/smoke.ts` 3-4번이 잡습니다.

**3. 감속은 곱하지 않고 더합니다** (`STATUS.slowCap`)

예전에는 화상(x0.9)을 오라(x0.55) 위에 **곱해서** x0.495 였습니다. 곱연산은 이미 느린 적일수록 새 감속이 실제로 깎는 양이 줄어서, **감속을 겹칠수록 하나하나가 표에 적힌 것보다 작게 일합니다.** 지금은 45% + 10% = **55% 감속**이라 표의 숫자를 그냥 더하면 결과가 나옵니다. 난이도 배율을 곱이 아니라 합으로 쌓기로 한 것과 같은 이유입니다.

`SKILLS.flame.burnSlowMul`(0.9, 배율)이 `burnSlow`(0.1, **감속량**)로 바뀌었습니다. 이름이 뜻하는 바가 달라졌으니 새로 감속을 넣을 때 헷갈리지 마십시오.

- **상한은 `STATUS.slowCap`(0.8) 입니다.** 더하기에는 끝이 없는데 100% 에 닿으면 그 적은 완전히 멈춥니다. 그건 감속이 아니라 무기한 기절이고, 넉백 기절만으로 보스가 40% 시간 정지이던 문제와 같은 자리입니다
- **지금 감속 출처는 둘입니다.** 감속 상태이상(`Enemy.slow`, 오라 등)과 화상입니다. 감속 상태이상끼리는 **매 틱 다시 걸리는 성질이라 가장 센 것 하나만 남기고**(`slowEnemy` 의 `Math.min`), 그 결과에 화상을 더합니다. **새 감속 출처를 붙일 때는 "더하는 것인가 갱신하는 것인가"를 먼저 정하십시오.** 매 틱 갱신되는 것을 더하게 두면 첫 1초 만에 상한까지 차오릅니다
- **플레이어가 장판에 받는 감속(`world.ts` 의 `p.slow`)은 그대로 `Math.min` 입니다.** 장판끼리는 같은 출처이고, 겹쳐 밟았다고 합쳐지면 장판 두 장에 갇혀 못 움직입니다

`scripts/smoke.ts` 3-5번이 **실제로 1초간 걸어본 거리**로 잽니다. 상수를 읽지 않고 재는 이유는 곱연산으로 되돌아가면 0.495 가 나오는데 그것까지 같이 잡기 위해서입니다.

### 열 번째 조정 (2026-08-12, 후반 곡선 펴기 · 스킬 10레벨)

사용자가 직접 플레이하며 **"시간이 지날수록 체감 난도가 가파른 곡선이 된다"**고 지적한 것을 쫓아간 결과입니다. 원인이 셋이었고 전부 15분에 겹쳤습니다.

1. **`LATE_SCALING` 이 적 체력 상승 기울기를 정확히 2배로 만들었습니다.** 15분까지는 `1 + 0.2 x 분` 이라 기울기가 분당 0.35인데, 15분부터 **이미 쌓인 x4 에 다시** `1 + 0.1 x (분-15)` 를 곱해서 분당 0.7이 됐습니다. 배율에 배율을 곱하면 그 경계에서 반드시 꺾입니다
2. **레벨 성장이 감속합니다.** `xpToNext` 가 레벨의 제곱(`0.6l²`)이라 앞 15분에 57레벨, 뒤 15분에 20레벨입니다. 공격력 성장이 +49% → +12% 로 4분의 1이 됩니다
3. **스킬이 만렙에 닿아 멈췄습니다.** 상한이 7이던 때는 20분쯤 다 채워져서 그 뒤로 스킬 성장이 0이었습니다

**고친 것 둘입니다.**

- **`LATE_SCALING` 을 통째로 없앴습니다.** `TIME_SCALING` 이 상한도 곱하기도 없이 **분당 +0.2 로 처음부터 끝까지** 갑니다. 30분 적 체력 x10 → **x7**. 속도만은 예외로 15분 이후 분당 +0.01 인데, 원래도 선형이고 "보고 피할 수 있다"를 지키려 일부러 느리게 잡은 값이라 그대로 뒀습니다
- **스킬 상한 7 → 10.** 만렙 위력은 예전 7레벨 값 **그대로 두고 곡선만 9칸으로 늘렸습니다** (perLevel 을 6이 아니라 9로 나눔). 최종 화력까지 같이 올리면 방금 잡은 곡선이 다시 틀어지고 무엇이 원인인지 알 수 없게 됩니다. 그리고 이 방식 자체가 3번을 풉니다. **판이 끝날 때까지 스킬이 계속 오릅니다**

결과입니다 (난이도 9 · 상점 전부 구매 · `npm run levelcurve -- 30 3 9`).

| 시각 | 1분 | 5분 | 10분 | 15분 | 20분 | 25분 | 30분 |
|---|---|---|---|---|---|---|---|
| 상대 난도 (전) | x1.00 | x1.49 | x1.88 | x2.24 | x3.23 | x4.13 | **x5.04** |
| 상대 난도 (후) | x1.00 | x1.40 | x1.76 | x2.20 | x2.59 | x2.97 | **x3.32** |
| 분당 처치 (전) | 87 | 155 | 509 | 467 | 415 | 416 | **357** |
| 분당 처치 (후) | 89 | 158 | 425 | 426 | 390 | 407 | **440** |
| 화면의 적 (전) | 13 | 18 | 42 | 42 | 45 | 51 | **67** |
| 화면의 적 (후) | 11 | 20 | 49 | 51 | 53 | 46 | **49** |

기울기가 **분당 +0.083 → +0.187 (2.25배로 꺾임)** 에서 **+0.080 → +0.075 (사실상 같음)** 이 됐습니다. 분당 처치가 더 이상 줄지 않고 화면의 적도 안 쌓입니다.

**반드시 지킬 것 둘입니다.**

- **`TIME_SCALING` 에 "몇 분 이후부터" 같은 구간을 다시 넣지 마십시오.** 구간을 나누면 그 경계에서 반드시 꺾이고, 플레이어 성장은 매끄럽게 이어지므로 그 꺾임이 그대로 체감됩니다. `scripts/smoke.ts` 5번이 **앞 15분과 뒤 15분의 체력 기울기가 같은지**를 직접 잽니다
- **`SKILL_MAX_LEVEL` 을 바꾸면 `SKILLS` 의 perLevel 을 전부 같이 고치십시오.** perLevel 은 "만렙에서 얼마"를 `상한-1` 로 나눈 값입니다. 상한만 올리면 만렙 위력이 같이 뛰어서, 성장 곡선이 아니라 최종 화력을 건드린 셈이 됩니다. 실제로 이번에 유탄 한 줄을 빠뜨려 32% 가 튀었고, `smoke.ts` **3-1번(만렙 위력 표)**이 그걸 잡았습니다. 그 시험은 총합 8종과 함께 "산탄이 2위의 두 배를 넘지 않는다" 같은 **결정**도 지킵니다

**업적 "완성형"은 지금 사실상 불가능합니다. 알면서 그냥 뒀습니다 (2026-08-12 보류 결정).**

스킬 4칸을 전부 만렙(10)으로 채우려면 선택이 40회, 즉 **Lv.120** 이 필요한데 30분 완주가 Lv.77 입니다. 두 개 만렙은 22회(Lv.66), 세 개는 31회(Lv.93)입니다. 그냥 두면 **업적 진행도 100% 가 영영 안 됩니다.**

**그런데도 조건을 낮추지 않은 이유는, 이 업적을 가능하게 만드는 방법이 조건을 낮추는 것 말고도 있기 때문입니다.** 레벨업 속도, 스킬 선택 간격, 판 길이, 경험치 곡선 중 무엇이든 손대면 도달 가능해집니다. 앞으로 요소가 더 붙을 예정이라 지금 조건을 낮추면, 나중에 도달 가능해졌을 때 이미 쉬워진 업적이 남습니다. **되돌리기 쉬운 쪽(조건은 그대로, 진행 방식이 바뀌면 자연히 풀림)을 골랐습니다.**

**요소를 추가한 뒤에는 반드시 다시 보십시오.** 그때도 여전히 불가능하면 그때 조건을 낮추면 됩니다. 판단 기준은 하나입니다. **30분 판에서 Lv.120 에 닿을 수 있는가.**

### 아홉 번째 조정 (2026-08-12, 스킬 14종 전면 재조정)

**스킬 상한이 5 → 7 레벨이 되고, 스킬 선택이 3레벨마다로 촘촘해졌습니다.** 유틸에 **긴급 의약품**이 붙어 스킬이 13종 → 14종입니다. 수치는 전부 `SKILLS` 에 있습니다.

**상한을 올릴 때는 `LEVEL.skillLevels` 를 반드시 같이 보십시오.** 이것이 이번 작업에서 가장 중요한 발견입니다. 스킬 4칸을 채우는 데 필요한 선택 횟수는 `4 + (상한-1) x 4` 인데, 판당 오는 선택 횟수가 그보다 훨씬 적으면 위쪽 레벨은 **존재하지 않는 것과 같습니다.** 상한 7 이면 28회가 필요합니다.

`scripts/levelcurve.ts` 로 실제 레벨 곡선을 재봤습니다 (무적 봇 30분 완주, 난이도 0).

| 시각 | 1분 | 3분 | 5분 | 10분 | 15분 | 20분 | 25분 | 30분 |
|---|---|---|---|---|---|---|---|---|
| 첫 판 | 5 | 11 | 16 | 30 | 40 | 47 | 52 | **56** |
| 상점 전부 구매 | 7 | 13 | 18 | 32 | 41 | 48 | 54 | **59** |

30분 완주가 Lv.55~59 입니다. 여기서 규칙별 선택 횟수가 갈립니다.

| 규칙 | 선택 횟수 | 만렙 가능 개수 |
|---|---|---|
| 옛 표 (3, 6, 10, 15, 21, 28, 36, 45, 55, 66, 78, 91) | 9회 | 0개 |
| 6레벨마다 | **9회 (옛 표와 같음)** | 0개 |
| **3레벨마다 (현재)** | **19회** | 2개 |
| 2레벨마다 | 29회 | 4개 전부 |

**"6레벨마다"가 아무것도 안 바꾸는 이유**는 옛 표가 앞은 촘촘하고 뒤는 벌어져 있어서 상쇄되기 때문입니다. 그리고 **전부 만렙이 되면 안 됩니다.** 4개를 다 채울 수 있으면 빌드라는 것이 사라집니다. 3레벨마다는 "두 개를 만렙으로 몰거나 네 개를 고르게 올리거나"의 갈림길을 만들려고 고른 값입니다.

스킬별로 바뀐 것입니다.

| 스킬 | 바뀐 것 |
|---|---|
| 산탄 | 위력 만렙 220% · 레벨당 2발씩 (만렙 17발) |
| 스나이퍼 | **관통 삭제 · 위력 2배 · 체력 가장 많은 적** 단일 저격 (열한 번째 조정에서 다시 손봤습니다) |
| 화염방사기 | **화상 중 재생 정지 + 이속 감소** · 레벨당 반각 1도 (열한 번째 조정에서 다시 손봤습니다) |
| 레이저 | 폭 레벨당 +5 (만렙 45) · 위력 x1.1. **유일한 전관통** |
| 유탄 | 탄속 2배(700) · 반경 레벨당 +11 (만렙 158 = 가로의 24.7%) |
| 추적 미사일 | 위력 x1.2 · 쿨 2.5 · **방패에 막힘** |
| 체인 | 위력 x1.2 · **마비 0.5초** |
| 회전 궤도 | 반경 70 → **110 고정**(레벨과 무관) · 타격 간격 0.32 → 0.20 |
| 지뢰 | 한 번에 까는 개수 **5개에서 상한** · 그 뒤로는 위력만 |
| 오라 | 반경 120 + 레벨당 12 (만렙 192) · 지속 증가 0.5 → **0.4** |
| 넉백 폭발 | 피해 3배 · 기절 2초 · **최대 체력 20% 소모** |
| 시간 감속 | **적탄도 같이 느려집니다** |
| 긴급 의약품 | 신규. 쿨 20초 · 최대 체력 20%(만렙 38%) 회복 |

**회전 궤도의 각속도는 3.1 그대로 두어야 합니다.** 한 적이 맞는 횟수는 "구체 수 ÷ 한 바퀴 도는 시간"이라 반경이 안 들어갑니다. 반경을 키웠다고 각속도를 낮추면 주기가 길어져 **타격이 36% 줄어듭니다.** 반경을 다시 만지면 `hitInterval` 만 `0.32 x (70 / 새 반경)` 으로 맞추십시오.

**보스는 상태이상을 70% 덜 받습니다** (`STATUS.bossStatusResist`). 넉백만으로 보스가 40% 시간 정지이고, 쿨감을 상한(60%)까지 올리면 아예 안 움직이기 때문입니다. **화상은 제외합니다.** 화상은 상태이상이라기보다 지속 피해라, 넣으면 "보스에게는 화력이 안 통한다"가 됩니다.

**`e.stun` 과 `e.slow` 를 직접 건드리지 말고 `w.stunEnemy` / `w.slowEnemy` 를 쓰십시오.** 보스 저항이 빠진 곳이 하나만 생겨도 그 스킬 하나로 보스가 통째로 멈춥니다.

**넉백의 체력 소모는 `w.spendHp` 로 갑니다.** `damagePlayer` 를 안 쓰는 이유가 셋입니다. 무적으로 막히면 안 되고(대가는 반드시 치러야 합니다), 무적 시간이 새로 켜지면 안 되며(대가를 치를수록 공짜 무적이 붙습니다), "한 대도 안 맞았다" 업적이 내가 낸 대가로 끊기면 안 됩니다. **최대 체력 기준입니다.** 현재 체력의 20% 로 하면 계속 반감이라 절대 안 죽어서 대가가 아니게 됩니다.

**넉백과 긴급 의약품은 같이 들 수 없습니다** (유틸 1칸). 의도한 것입니다. 넉백 빌드는 `regen` 스탯과 보스 처치 회복으로 버텨야 합니다.

**오라의 지속 증가를 0.5 → 0.4 로 낮춘 이유**는 상한이 7이 되면서 0.5 로 두면 만렙 지속이 정확히 7.0초로 쿨다운과 같아져 가동률이 100% 가 되기 때문입니다. 지속시간도 쿨다운도 둘 다 의미를 잃습니다.

**봇은 유틸을 판단해서 씁니다** (`scripts/survival.ts` 의 `shouldUse`). 넉백이 체력을 태우게 된 뒤로 쿨마다 누르는 봇이 다섯 번 만에 자살해서, 생존 시간이 스킬 성능이 아니라 "넉백을 뽑았는가"로 정해졌습니다. 새 유틸에 대가나 조건이 붙으면 이 함수도 같이 보십시오.

**게임이 실제로 많이 쉬워졌습니다.** 난이도 0 은 상점을 다 사면 6판 중 5판이 30분을 완주하고, 사용자가 직접 해본 소감도 "난이도 9가 매우 쉬워졌다"였습니다. 원인은 둘이 겹친 것입니다. 스킬 만렙 총합이 산탄 37.4 · 체인 31.8 · 지뢰 26.0 · 미사일 21.1 로 예전 최대(8.82)의 3~4배이고, 거기에 선택 횟수가 9회 → 19회로 늘어 **그 위력에 실제로 닿게** 됐습니다.

**난이도 표(`DIFFICULTY_STEPS`)를 다시 봐야 합니다.** 적 체력은 분당 +10% 선형이라 30분에 4배인데 스킬은 그보다 빠르게 자랍니다. 되돌릴 손잡이는 순서대로 `SPAWN.rateStart`, `TIME_SCALING.hpPerMinute` 입니다. 스킬 위력을 다시 깎는 것은 마지막 수단으로 두십시오. 이번 조정의 목적이 "레벨을 올릴 이유를 만드는 것"이었는데 그것부터 되돌리면 앞뒤가 안 맞습니다.

**아직 사용자에게 확인받지 못한 것**: 난이도 9에서 구체적으로 무엇이 시시했는지입니다. 후보는 체인·넉백의 군중제어(멈춰놓고 때리기), 회전 궤도 8구체·반경 110, 화력 총량, 그리고 코인 획득량(판당 2,861이면 60판에 상점이 다 찹니다) 넷입니다. **답을 듣기 전에 수치를 되돌리지 마십시오.** 어느 것이 원인인지 모르는 채로 여러 개를 동시에 깎으면 무엇이 들었는지 알 수 없게 됩니다.

### 여덟 번째 조정 (2026-08-11, 업적 화면 · 설정 · 지뢰)

1. **업적 목록 정리**: 사슬 업적은 다음 한 칸만(입문 -1 은 예외로 따로), 단계형은 다음 목표 하나와 지금 값만, 히든은 전부 맨 아래 한 덩어리. 자세한 것은 위 "업적 시스템" 항목에 있습니다
2. **설정 화면**(메인 우측 상단 톱니바퀴): 테스트용 초기화 두 개. 위 "설정 화면" 항목 참고
3. **"뒤지게 빠르네" 기준 변경**: 화면의 적 → 적 표 전체 (`fastestEnemySpeed`)
4. **"마우스 조작은 안해요" 기준 변경**: 마우스 움직임 → 화면 클릭
5. **지뢰 개수 상한 삭제 + 흩뿌리기**: 아래 참고
6. **업적 판정이 판 밖에서도 돕니다**: 아래 참고
7. **마지막에 고른 난이도가 저장에 남습니다** (`SaveData.lastDifficulty`): 난이도 화면이 0 이 아니라 그 자리에서 시작합니다. 10 을 파고 있는 사람이 매판 화살표를 열 번 누르는 일이 없어야 합니다. **`maxDifficulty` 와 다른 값입니다.** 저건 "어디까지 열렸는가"고 이건 "직전에 무엇을 골랐는가"라, 낮은 난이도로 내려가서 논 뒤에도 그 자리에서 이어집니다. 고르는 그 순간 저장합니다(판이 끝날 때 저장하면 도중에 새로고침한 판이 사라집니다)
8. **정예 바보적의 탄속 강화를 없앴습니다** (`eliteBulletSpeed: 430` 삭제): 정예든 아니든 230 입니다. 바보적은 **예고 없이** 벽에서 쏘는데 거기에 탄속까지 붙으니 보고 반응할 수가 없었습니다. 무엇보다 조준선을 2초 그리고 쏘는 원거리적의 탄(300)보다 빨랐습니다. **미리 알려주는 적의 탄이 예고 없는 적의 탄보다 느리면 예고라는 장치 자체가 의미를 잃습니다.** 정예의 몫은 갈래 수(1 → 3)로 냅니다. `scripts/smoke.ts` 10-4-11번이 "정예 바보 탄속 = 일반과 같음"과 "정예 원거리적보다 느림" 둘 다 잡습니다. 뒤쪽은 상수를 읽지 않고 **실제로 쏘게 해서 잽니다** (나중에 정예 원거리적에 탄속 강화가 붙어도 규칙이 그대로 지켜지도록)
9. **스탯 영구 강화가 20단계까지 갑니다** (`PERM_MAX_LEVEL`): 그전에는 3~5단계였습니다. 앞 단계 값은 그대로 두고 그 뒤를 `PERM_COST_GROWTH`(1.25)배씩 이어 붙여 만듭니다(`permCosts`). 손으로 120개를 적으면 곡선이 항목마다 어긋나므로 **곡선을 조정할 때는 그 두 상수만 만지십시오.** 한 항목을 끝까지 채우는 데 체력 27,895 · 연사 39,550 코인이고, 여섯 항목 전부면 **172,295 코인**입니다. 최종 효과는 체력 +300 · 공격력 +40 · 이동속도 +160 · 연사 +3.0 · 코인 범위 +500 · 치명타 +60% 입니다
10. **난이도 9 이상에서도 정예 분열적이 재분열합니다**: 아래 "전원 정예화 안전장치" 참고

**업적 판정은 판 안에서만 돌았습니다.** 상점에서 다섯 번째 물건을 사도 알림이 안 뜨고, 다음 판을 시작해야 그제서야 떴습니다. 무엇 때문에 받았는지 알 수 없는 알림입니다. 두 곳을 고쳤습니다.

- **상점은 산 직후, 다시 그리기 전에 훑습니다** (`showShop` 의 `onChange`). 다시 그리기 뒤로 가면 방금 받은 보상이 화면의 코인에 안 잡혀서, 상점을 나갔다 들어와야 맞는 숫자가 보입니다
- **화면을 바꿀 때마다 한 번씩 훑습니다** (`open`). 판 밖에서 열리는 업적은 판정할 자리가 여기밖에 없습니다
- **`sweepAchievements` 에서 `save = loadSave()` 를 걷어냈습니다.** `checkAchievements` 는 넘겨준 객체를 그 자리에서 고치므로 다시 읽을 이유가 없는데, 다시 읽으면 내용은 같지만 **다른 객체**가 됩니다. 그러면 지금 열려 있는 상점 화면이 낡은 객체를 계속 고쳐서, 그다음 구매가 방금 받은 업적 보상을 지우고 덮어씁니다

**지뢰(`SKILLS.mine`)에는 동시 개수 상한이 없습니다.** 예전에는 8개가 상한이라 레벨 5(한 번에 5개)는 두 번째 설치부터 발동 자체가 막혔습니다. **성장시킬수록 못 쓰게 되는 스킬**이었습니다.

대신 2개째부터는 발밑이 아니라 주변에 흩뿌립니다. 한자리에 겹쳐 깔면 개수가 늘어도 실제로 덮는 넓이가 안 늘어서, 그것도 레벨이 헛도는 것과 같습니다.

- **첫 한 개는 반드시 발밑입니다.** "지나온 자리에 남긴다"가 이 스킬의 정체라, 전부 흩어지면 쫓기면서 뒤에 두고 가는 사용법이 사라집니다
- **흩어지는 반경(`spread`)은 `BASE_STATS.pickupRange`(75) 를 그대로 씁니다.** 코인 획득 범위의 **기본값**이지 지금 스탯이 아닙니다. 스탯을 읽으면 코인 자석을 살수록 지뢰가 넓게 퍼져서, 지뢰와 아무 상관 없는 강화가 지뢰를 약하게 만듭니다
- 원 안이 고르게 차도록 반경에 `sqrt` 를 씌웁니다. 안 씌우면 가운데로 몰립니다
- 규칙은 `scripts/smoke.ts` 3-2번이 자동으로 잡습니다

### 다섯 번째 조정 (2026-08-06, 난이도 전면 재설계)

**난이도가 -1 ~ 15 로 확정됐습니다. 16 이상은 없습니다.** 예전의 "표를 다 쓰면 무한 심화"(`beyondTableMul`)는 없앴습니다.

**난이도 선택 화면이 처음부터 열립니다.** -1(입문) 과 0(기본) 은 언제나 고를 수 있고, 1 부터는 바로 아래 난이도로 요구 시간을 버텨야 차례로 열립니다. 쉬운 난이도를 "15분 도달" 뒤에 잠가두면 정작 그것이 필요한 사람이 못 쓰기 때문입니다.

**배율은 곱이 아니라 합으로 쌓입니다.** 1단계 체력 +20% 위에 3단계 체력 +10% 가 오면 최종은 +30%(x1.30) 이지 x1.20 x 1.10 = x1.32 가 아닙니다. 표에 적힌 퍼센트를 그냥 다 더한 값이 최종입니다. 그래야 표만 보고 최종 수치를 암산할 수 있고, 그래야 표의 값을 손볼 수 있습니다. `meta/difficulty.ts` 의 `addMul` 이 이걸 담당하며, `scripts/smoke.ts` 8번이 자동으로 잡습니다.

단계별 효과는 `DIFFICULTY_STEPS` 를 그대로 읽으십시오. 누적 결과만 옮기면 아래와 같습니다.

| lv | 체력 | 공격력 | 접촉 | 속도 | 적탄 | 스폰율 | 코인 | 보스코인 | 클리어 |
|---|---|---|---|---|---|---|---|---|---|
| -1 | x0.90 | x0.90 | - | - | - | x0.75 | x0.70 | - | 15분 |
| 0 | - | - | - | - | - | - | x1.00 | - | 15분 |
| 3 | x1.30 | x1.20 | - | - | - | - | x1.45 | x1.20 | **30분** |
| 5 | x1.30 | x1.30 | - | x1.10 | x1.10 | x1.25 | x1.75 | x1.20 | 30분 |
| 8 | x1.75 | x1.50 | - | x1.20 | x1.10 | x1.25 | x2.20 | x1.44 | 30분 |
| 10 | x1.75 | x1.50 | - | x1.20 | x1.10 | x1.50 | x2.50 | x1.73 | 30분 |
| 15 | **x2.10** | **x1.70** | **x1.50** | x1.20 | x1.10 | x1.50 | x3.25 | x2.00 | 30분 |

15단계 최종값의 근거입니다. 체력 20+10+30+15+10+25 = **110%**, 공격력 20+10+10+10+20 = **70%**, 속도 10+10 = **20%**, 스폰율 25+25 = **50%**.

배율 말고 "장치"가 붙는 단계는 여섯입니다.

| lv | 장치 |
|---|---|
| 3 | 클리어 조건 +15분 (그 뒤로 계속 30분) |
| 6 | 1분마다 적 5마리 웨이브 |
| 8 | 레벨업 스킬 선택지 3장 → 2장 |
| 9 | **모든 적이 정예** + 정예 표식 사라짐 |
| 11 | 웨이브가 5분부터 1분마다 15마리로 **교체** |
| 12 | 3분부터 30초마다 자폭병 5마리 (자폭병 속도 x0.8 · 공격력 x1.2) |
| 13 | 돌진적 기절 없음 + 돌진 쿨타임 없음 (예고는 그대로) |
| 14 | 분열할 때 6방향 탄막 |
| 15 | 무적 바보적 1마리 상시(타겟 불가 · 플레이어 쪽 5갈래 · 탄속 x0.5 · 이동 x1.5) + **일반** 바보적 8방향 탄막 + 겁쟁이 인내 x0.5 + 장판 지속 x1.2 |

**코인**은 난이도 1단계마다 +15%p 이고, **보스 코인은 난이도 3단계마다 +20%p** 로 따로 붙습니다 (`bossCoinPerLevels`, `bossCoinMul`). 이것도 합이라 15단계에서 x2.00 입니다.

### 일곱 번째 조정 (2026-08-09, 업적 준비: 보스 중복 · 선택 건너뛰기)

업적 시스템을 넣기 전에, 업적이 요구하는데 지금 규칙으로는 **불가능하던 두 가지**를 풀었습니다.

- **보스가 겹쳐서 등장합니다** (`BOSS.maxAlive` 3, `World.bossesAlive`). 예전 `bossAlive` 는 참/거짓이었고 한 마리라도 살아 있으면 다음 보스 카운트다운이 통째로 멈췄습니다. 못 잡으면 보스가 영영 끊기고 코인·회복까지 같이 끊기던 문제가 여기서 같이 풀립니다. 지금은 못 잡으면 **쌓입니다.** 상한을 둔 이유는 30분 판에서 여섯 마리가 겹치기 때문이고, 상한까지 찼을 때만 카운트다운이 멈춥니다. 보스는 등장 횟수마다 단단해지므로(`hpGrowthPerSpawn`) 겹치면 뒤에 온 쪽이 더 셉니다
- **스킬 선택 건너뛰기를 상점에서 팝니다** (`SKIP_UPGRADE`, 350 / 800 / 1400 / **3000 무제한**). 리롤(260 / 620 / 1100)보다 비쌉니다. 리롤은 "원하는 것을 뽑는" 도구인데 이건 "빌드를 좁게 유지하는" 도구라 판 전체의 성격을 바꾸기 때문입니다. **기획 12장의 "거절 제거"를 되돌린 것이 아닙니다.** 공짜 거절이 아니라 비싼 소모품이라 "무조건 하나를 고른다"는 기본 규칙은 그대로입니다. 레벨업 화면에서 S 키 또는 카드로 씁니다
- **마지막 단계(3000 코인)는 판당 횟수 제한이 사라집니다** (`SKIP_UPGRADE.unlimitedLevel`, `skipCount` 가 `Infinity` 를 돌려줍니다). 횟수제만으로는 "하나만 쓰는 빌드"가 성립하지 않아서입니다. 레벨 45 까지 스킬 선택이 8회 오는데 횟수제 상한은 3회라, 남는 5회를 유틸 카드가 우연히 떴는지에 맡기게 됩니다. 실력이 아니라 운이 결정하는 빌드는 업적으로 삼을 수 없습니다. 3000 은 상점에서 가장 비싼 항목입니다
- 규칙은 `scripts/smoke.ts` 13·14번이 자동으로 잡습니다

### 업적 시스템 (2026-08-09 구현 완료)

**명세와 전체 목록은 `업적.md` 에 있습니다.** 53개(히든 12개), 보상 총액 16,680 코인입니다. 여기에는 코드를 만질 때 알아야 할 것만 적습니다.

- **코인은 달성 즉시 지급하고 즉시 저장합니다** (`checkAchievements`). 판이 끝날 때 몰아주면 중간에 그만둔 판의 업적이 사라집니다. 업적은 판의 성과가 아니라 계정의 성과입니다
- **자학 업적에 코인을 줘도 되는 이유는 1회성이기 때문입니다.** "10초 안에 죽기"에 100 코인이 붙지만 두 번 받을 수 없어서 전략이 되지 않습니다. 파편 처치에 보상을 안 준 이유(매판 반복 가능)와 성질이 다릅니다. **반복 가능한 것에는 보상을 붙이지 마십시오**
- **알림은 오버레이 바깥(`#toasts`)에 붙습니다** (`src/ui/toast.ts`). `clearOverlay()` 는 화면을 바꿀 때마다 오버레이를 비우는데, 알림이 그 안에 있으면 게임오버 화면이 뜨는 순간 방금 딴 업적이 같이 사라집니다. 알림은 화면과 수명이 다르므로 자리도 달라야 합니다. `updateToasts` 는 메뉴에 있든 게임 중이든 매 프레임 돕니다
- **판 안에서 모으는 것은 `World.track`(`RunTrack`) 하나입니다.** 다만 **"지금 화면을 보면 알 수 있는 것"은 넣지 않습니다.** 보스 2마리나 탱커 3분 생존은 `w.enemies` 를 훑으면 그만이라 조건식에서 직접 봅니다. `track` 에 있는 것은 지나가면 사라지는 사건뿐입니다
- **누적값은 `save.achieveStats` 입니다.** `records` 에 이미 있는 것(총 처치, 총 판 수, 난이도별 최고 기록)은 여기에 넣지 않습니다. 같은 값을 두 곳에서 세면 반드시 갈라집니다
- **난이도 클리어와 누적 처치는 예전 저장에 소급됩니다.** 조건식이 `records` 를 그대로 읽기 때문입니다
- **조건식이 없는 업적은 밖에서 엽니다** (`unlockDirect`). 코나미 코드처럼 게임 상태로 표현되지 않는 것뿐입니다. `check` 가 없으면 저절로 열리지 않습니다
- **히든은 이름까지 가립니다.** 조건만 가리면 "무궁화 꽃이 피고 있는 듯한 느낌" 같은 이름에서 조건이 그대로 유추됩니다. **다만 개발자 모드에서는 그대로 보입니다** (2026-09-03). 조건을 확인하려면 무엇을 재는 업적인지 알아야 하는데 `???` 로 가려져 있으면 코드를 열어봐야 합니다. **가리는 것은 처음 겪는 사람을 위한 것이지 만드는 사람을 위한 것이 아닙니다.** `boot.ts` 6-2 번이 잡습니다
- **줄줄이 이어지는 업적은 화면에 다음 한 칸만 보입니다** (`AchieveDef.chain`, `visibleAchievements`). 난이도 클리어 17개가 통째로 늘어서면 못 깬 뒤쪽이 목록을 채우면서 정작 지금 노릴 것이 안 보입니다. 하나를 깨면 그 자리에 다음 칸이 나타납니다. **입문(-1)만 사슬 밖에 두어 항상 따로 보입니다.** 해금 사슬은 0 에서 시작하므로 -1 을 건너뛰고 0, 1, 2 를 깨는 것이 정상 경로인데, -1 을 사슬 맨 앞에 두면 그 한 칸 때문에 뒤쪽이 통째로 가려집니다. **진행도 분모(`achieveProgress`)는 가린 것까지 전부 셉니다.** 보이는 줄 수를 분모로 삼으면 사슬을 깰 때마다 분모도 같이 늘어 진행도가 안 움직입니다
- **단계형도 다음 목표 하나와 지금 값만 적습니다**: 여섯 단계를 한 줄에 늘어놓으면 무엇을 채우면 되는지가 그 안에 묻힙니다
- **히든은 찾았든 아니든 전부 맨 아래 한 덩어리입니다**: 이름도 조건도 없는 줄이 목록 사이에 섞이면 읽는 흐름이 끊깁니다. 묶음 머리(`.ach-sep`)에 찾은 수를 적어서 그 ??? 들이 무엇인지 한 번만 밝힙니다
- **"뒤지게 빠르네"의 기준은 화면이 아니라 적 표 전체입니다** (`fastestEnemySpeed`): 떠 있는 적만 재면 빠른적이 마침 없는 순간에 열립니다. 정예 배율까지 셈해서 가장 빠른 종류(정예 빠른적)를 기준으로 삼고, 돌진처럼 능력으로 나는 속도는 안 셉니다. **시간 강화와 난이도 배율도 안 넣습니다** (2026-08-16). 넣으면 기준선이 판이 갈수록 달아나서 같은 이동속도로도 1분에는 열리고 20분에는 안 열립니다
- **"마우스 조작은 안해요"는 클릭에서만 열립니다** (`RunTrack.mouseClicked`): `mousemove` 까지 세면 책상 위의 마우스를 스치거나 창을 옮기다 커서가 지나가기만 해도 깨집니다. **스킬 선택 클릭은 안 셉니다** (2026-08-16). 창이 떠 있는 동안은 `screen` 이 `playing` 이 아니라 저절로 빠지고, 창을 닫은 직후 `MOUSE_GRACE_MS`(400ms) 안의 한 번은 유예가 막습니다. 카드를 마우스로 고르는 것은 허용된 조작이라 그것 때문에 깨지면 안 됩니다
- **판정은 0.25초마다 한 번입니다** (`ACHIEVEMENT.checkInterval`). 조건식 하나가 예외를 던져도 게임이 멈추지 않도록 `reachedTier` 가 감쌉니다
- 전체 보상 규모는 `ACHIEVEMENT.coinMul` 하나로 조절합니다

### 여섯 번째 조정 (2026-08-09, 죽는 연출)

플레이어가 죽으면 판이 통째로 멈추고 **1초 동안 몸이 파편 수류탄처럼 터집니다.** 파편에 닿은 잡몹은 즉사합니다 (`DEATH_BURST`, `World.startDeathBurst` / `updateDeathBurst` / `shardHits`, 화면 상태 `dying`).

- **멈춤과 파편은 부딪히지 않습니다. 거기가 핵심입니다.** 적·적탄·장판·스폰을 전부 세우고 움직이는 것을 파편과 파티클만 남기면 "시간이 멈춘 자리에 파편만 날아간다"가 됩니다. `update` 는 `gameOver` 에서 곧장 빠져나가고 `updateDeathBurst` 만 돕니다
- **파편 처치에는 보상이 없습니다.** 코인도 경험치도 처치 통계도 붙지 않습니다. 주면 "잡몹 한가운데서 일부러 죽는" 것이 이득이 되어 마지막에 들이받는 플레이가 생깁니다. 그래서 `killEnemy` 를 타지 않고 지우기만 하며(분열·시체 폭발도 안 이어집니다), 데려간 수는 `World.shardKills` 에 담아 게임오버 화면에 한 줄로만 보여줍니다. **사용자가 업적 시스템을 나중에 붙이자고 했으므로 그때 이 값을 쓰십시오**
- **보스와 무적 개체는 파편으로 죽지 않습니다.** 체력 1600 인 보스가 파편 하나에 지워지면 "잡기 힘들면 옆에서 죽으면 된다"가 되어 보스 체력 2배 결정이 무너집니다. 난이도 15 의 무적 바보적은 못 죽는다는 것 자체가 그 적의 규칙이라 예외를 만들면 안 됩니다. 둘 다 불꽃만 튀고 지나갑니다 (`e.boss || e.immortal || e.invuln > 0`)
- **죽는 순간만 화면 흔들림 상한을 넘깁니다** (`SHAKE.deathMax` 30, `Effects.addDeathShake`). 상한을 9 로 조인 이유는 후반에 폭발과 처치가 몰려 계속 떨리는 것이었는데, 이건 판당 한 번이고 그 뒤로 게임이 끝나므로 그 문제와 무관합니다. 더하지 않고 **갈아끼웁니다.** 죽기 직전에 이미 흔들리고 있었다고 죽는 순간이 더 크게 흔들릴 이유는 없습니다
- **건너뛰기는 `wasPressed` 기준입니다** (`Input.anyPressed`, `DEATH_BURST.skipAfter` 0.25초). "누르고 있는가"로 하면 죽는 순간 방향키를 잡고 있던 손 때문에 연출이 그대로 넘어갑니다. 앞 0.25초를 막아두는 것도 같은 이유입니다
- **파편은 파티클이 아니라 별도 배열입니다** (`World.shards`, `Shard`). `Effects.particles` 는 판정이 없고 수명이 짧아서 얹을 수 없습니다. 파편은 부채꼴(`r.cone`)로 그리고 플레이어와 **같은 두 색**을 씁니다. 색이 달라지면 "내가 쪼개진 것"이 아니라 새로 생긴 무언가로 보입니다
- **부활이 남아 있으면 이 연출은 안 나옵니다.** `damagePlayer` 에서 이미 갈래가 나뉘어 있습니다
- 규칙은 `scripts/smoke.ts` 12번이 자동으로 잡습니다 (보상 없음 · 보스/무적 생존 · 건너뛰기 시점)

### 네 번째 조정 (2026-08-05, 정예·보스 전면 손질)

정예 14종 중 **12종에 고유 능력**이 붙었습니다 (미라·은신은 2026-08-16 에 채웠습니다). 공통 공격력 배율은 1.2 → **1.5**. 표는 아래 "정예 고유 능력 요약"에 있습니다.

보스는 체력이 전부 **2배**(800 → 1600)가 되고 셋의 성격을 갈랐습니다. 포식자는 근접, 폭격기는 탄막, 군체왕은 잡몹입니다. 수치는 `BOSS_PREDATOR` / `BOSS_BOMBARD` / `BOSS_SWARM` 으로 나눠 두었습니다.

**그 과정에서 실제 버그를 두 개 잡았습니다.**

1. **소환적이 멀리 있으면 소환을 아예 안 했습니다.** `wander()` 가 쓰는 `state.timer` 와 소환 간격이 같은 칸을 다투는데, wander 가 1.5초마다 다시 채워버려서 소환 조건이 영영 성립하지 않았습니다. 도주 사거리(430) 안에 플레이어가 들어왔을 때만 소환하고 있었습니다. 소환 간격을 `timer3` 으로 옮겼습니다
2. **장판 피해가 무적 시간을 무시했습니다.** 원래 `hazardDps: 0` 이라 드러나지 않던 문제입니다. 정예 장판에 피해가 붙으면서 대시로도 못 빠져나가게 되어, 무적을 존중하도록 고쳤습니다

### 세 번째 조정 (2026-08-05, 플레이 피드백)

1. **회전 궤도는 항상 켜져 있습니다**: 쿨다운과 지속시간을 없앴습니다. 쿨이 지속시간보다 짧아지면 구체가 겹쳐 쌓이던 문제가 원인입니다. 대신 공격력을 예전 가동률(83%)만큼 낮춰 잡았습니다 (0.65 → 0.54)
2. **레벨업 선택지에 업그레이드가 확정으로 끼던 것 수정** (`MAX_UPGRADE_CHOICES_PER_ROLL`): 카드마다 따로 굴려서 0.35 가 실제로는 72% 였습니다. 지금은 판마다 한 번만 굴려 35% 입니다. 카드 순서도 섞습니다
3. **자폭적 피격 점화 복구**: 맞으면 다시 2.5배로 빨라지고 4초 뒤 터집니다. 근접 점화(0.8초)와 시체 폭발(2초)은 그대로 남습니다. 도화선이 둘(`fuse` / `contactFuse`)인 이유는 방아쇠 성격이 달라서입니다
4. **돌진적 인식 사거리 삭제** (`triggerRange` 제거): 화면 어디에 있든 쿨이 돌면 예고합니다
5. **돌진적 벽 문제 두 가지 수정**: 벽 판정을 "붙어 있는가"에서 "벽 쪽으로 가고 있는가"로 바꿨고, 벽에서 `wallClearance`(70) 안쪽이면 안쪽으로 걸어 나온 뒤에 예고합니다
6. **돌진 예고 통로가 차오릅니다**: 진하기로 알리던 것을 적 쪽에서부터 실제로 채우는 방식으로 바꿨습니다
7. **정예 돌진적 예고 0.7배 → 0.4배**
8. **화면 흔들림 전체 축소** (`SHAKE`): 배율 0.3, 상한 34 → 9

### 두 번째 조정 (2026-08-05, 사용자 지시)

1. **자폭적 재설계**: 방아쇠가 "피격"에서 "근접"과 "처치" 둘로 바뀌었습니다. 아래 결정 목록 참고
2. **정예 탱커 체력 5배 → 3배 + 초당 5% 재생** (`ELITE_TRAITS.tank.hpRegenRatio`)
3. **정예 원거리적 조준 0.5배 → 0.4배**
4. **겁쟁이 인내 30초** (`ENEMY_PARAMS.coward.patienceTime`): 30초를 살아남으면 사거리와 무관하게 계속 달려듭니다. 그 상태는 몸 주변 링으로 보입니다 (`cowardEnraged`)

5. **체인 라이트닝 강화**: 감쇠 `falloff` 0.85 → **0.92**, **연쇄 사거리(`jumpRange`) 삭제**. 1타(`damage`)는 그대로 둡니다
6. **레벨업 알림 순서**: 스킬 선택창이 뜨는 레벨에서 오른 스탯이 창 뒤에서 사라지던 것을 고쳤습니다. 창을 닫으면 스탯이 한 줄씩 뜨고 그 뒤에 획득한 스킬이 뜹니다 (`World.notices`, `LEVEL_NOTICE`)

### 그 전에 끝낸 것 (2026-08-05)

**1. 정예의 종류별 체력·속도 배율** (`ELITE_TRAITS` 의 `hpMul` / `speedMul`)

| 적 | 속도 | 체력 | 비고 |
|---|---|---|---|
| 탱커 | x0.8 | **x5** | 더 느리고 훨씬 단단한 벽이 됩니다 |
| 빠른 | **x1.2** | x2 | 체력은 덜 오르고 속도로 위협합니다 |
| 그 외 | 현행 유지 | 현행 유지 | 공통 `ELITE` 배율 그대로 (체력 1.5배, 공격력 1.2배) |

종류별 값이 있으면 공통 `ELITE` 값을 **대체**합니다. 곱하는 것이 아닙니다. 탱커 정예 체력은 기본 체력의 5배이지 7.5배가 아닙니다. 읽는 곳은 `enemies/elite.ts` 의 `eliteStatMul` 하나뿐이고 `world.spawnEnemy` 가 그것을 씁니다. 크기·공격력·경험치는 지금도 공통 `ELITE` 를 그대로 씁니다.

**2. 자폭적은 스폰 후 1초 대기했다가 출발** (`ENEMY_PARAMS.bomber.spawnDelay`)

대기 시간은 `state.timer2` 에 담습니다(`timer` 는 도화선이 이미 씁니다). 대기 중에는 몸 주변 링이 조여들어서 언제 출발하는지 눈으로 보입니다. 점화되면 대기는 즉시 취소됩니다. 맞았다는 것 자체가 플레이어가 이미 알아챘다는 뜻이라 더 기다려줄 이유가 없습니다.

### 그 뒤에 할 것

1. **직접 플레이하고 밸런스를 조정합니다.** 수치는 전부 `src/data/balance.ts` 한 곳에 있습니다
2. **정예 고유 강화(`ELITE_TRAITS`)를 나머지 종류까지 채웁니다.** 사용자가 직접 방향을 잡기로 한 부분입니다. 현재 탱커(체력 5배·속도 0.8배) · 빠른(체력 2배·속도 1.2배) · 원거리(조준 0.5배) · 돌진(예고 0.7배) 넷입니다. 새 항목은 `EliteTrait` 에 필드를 더하고 읽는 쪽에서 `eliteMul` 을 쓰면 됩니다
3. **난이도 표(`DIFFICULTY_STEPS`)를 플레이로 검증합니다.** 특히 9단계(전원 정예)와 13단계(돌진 쿨 없음)가 실제로 어느 정도인지, 15단계의 무적 바보적이 방해로 느껴지는지 봐야 합니다
4. **보스 3종의 난도를 플레이로 확인합니다.** 특히 군체왕은 나선 탄막과 소환이 겹쳐서 화면이 가장 복잡해집니다
5. `기획.md` 11장의 "아직 안 정한 것" 중 남은 것을 플레이로 결정합니다 (장판 지속, 해금 시간표, 스킬 선택 간격)
6. 사운드

## 정예 고유 능력 요약 (2026-08-05)

공통 배율(`ELITE`)은 체력 x1.5 · **공격력 x1.5** · 크기 x1.3 · 경험치 x2.5 · 코인 확정 1개입니다.
종류별 값(`ELITE_TRAITS`)이 있으면 공통값을 **대체**합니다. 곱하지 않습니다.

| 적 | 체력 | 속도 | 공격력 | 고유 능력 |
|---|---|---|---|---|
| 기본 | x1.5 | x1 | x1.5 | 없음 |
| 빠른 | x2 | **x1.4** | x1.5 | 없음 |
| 탱커 | **x3** | **x0.8** | x1.5 | 초당 최대 체력 5% 재생 |
| 원거리 | **x2** | x1 | x1.5 | 조준 x0.4 (2.0초 → 0.8초) |
| 겁쟁이 | x1.5 | x1 | x1.5 | 인식 사거리 x1.5 · 돌진 속도 x1.5 |
| 바보 | **x2** | **x1.2** | **x2** | 튕길 때 3갈래 (일반은 1발). **탄속은 일반과 같습니다** |
| 자폭 | x1.5 | x1 | x1.5 | 시체 폭발 x0.5 시간 · 폭발 반경 x1.5 |
| 분열 | x1.5 | x1 | x1.5 | **크기 x1.5** · 분열체가 한 번 더 분열 (1 → 3 → 9) |
| 돌진 | x1.5 | x1 | x1.5 | 예고 x0.4 (3.0초 → 1.2초) |
| 장판 | x1.5 | x1 | x1.5 | 장판 x2 넓이 · 감속 0.5 → 0.7(약화) · 1초 뒤부터 0.5초마다 공격력 x0.7 피해 |
| 소환 | x1.5 | x1 | x1.5 | 부르는 하수인이 **정예**가 됩니다. 그 외 행동은 일반과 같습니다 |
| 방패 | x1.5 | x1 | x1.5 | 내구도 = 체력 x1.2 · 방패 있을 때 피해 x0.5 · 깨진 뒤 피해 x1.2 |
| 미라 | x1.5 | x1 | x1.5 | 부활 대기 x0.5 (3.0초 → 1.5초) |
| 은신 | x1.5 | x1 | x1.5 | 드러나는 거리 x0.5 (230 → 115). 더 가까이 와야 보입니다 |

소환적이 부르는 하수인의 **종류**는 스폰할 때 한 번 정해집니다 (`summoner.minionPool` 중 하나). 정예와 일반의 차이는 그 하수인이 정예인가뿐입니다.

## 반드시 지킬 것

1. **모든 밸런스 수치는 `src/data/balance.ts` 한 곳에만 둡니다.** 다른 파일에 숫자를 직접 쓰지 마십시오
2. **은신적은 드러난 1초 동안에만 타겟팅 가능합니다.** `skills/targeting.ts`의 `canTarget`이 이걸 담당합니다. 새 스킬을 만들 때 이 함수를 거치지 않으면 규칙이 깨집니다
3. **정예는 시각적으로 반드시 구분합니다** (붉은 이중 링 + 크기 30% + 바닥 오라 + 머리 위 뿔 + 항상 보이는 체력바). 첫 플레이에서 "정예인지 판단이 힘들다"는 지적이 나온 부분이므로 약하게 되돌리지 마십시오
4. **적의 색은 위험도, 모양은 행동 유형입니다.** 접촉 피해가 큰 적일수록 강렬한 난색(주황 → 빨강 → 진홍)을 쓰고, 안전한 적은 차분한 한색으로 물러납니다. 같은 실루엣(변 수 + 표식)을 두 종이 나눠 쓰면 안 됩니다. 이 두 규칙은 `scripts/smoke.ts` 10번이 자동으로 잡습니다. 색을 만질 때는 반드시 `npm run check`를 돌리십시오
5. **적 색은 `registry.ts`의 `color` / `accent` 한 곳에만 씁니다.** 표식이나 장판 색을 render 쪽에 직접 쓰면 색을 바꿀 때 따로 놀게 됩니다
6. **적탄만은 예외로 전부 같은 빨강입니다** (`ENEMY_BULLET.color`). 쏜 적이 누구든 상관없습니다. 날아오는 탄에 필요한 정보는 "맞으면 아프다" 하나뿐이라, 적 색을 따라가면 초록 탄과 분홍 탄이 섞여 날아와 매번 다시 판단해야 합니다. 조준선은 "누가 어디서 쏘는가"라 적 고유색을 유지합니다
7. **스킬 타겟팅 규칙은 스킬마다 다릅니다.** 전부 "가장 가까운 적"으로 만들면 소환적을 아무도 못 잡고 빌드의 의미가 사라집니다
8. **누를 수 없는 정보 카드에는 `card({ info: true })` 를 씁니다.** `disabled` 만 쓰면 상점의 "못 사는 항목" 흐림이 걸려 화면 전체가 희미해집니다 (도감이 실제로 그랬습니다)
9. **예고와 실제 발동 시점은 같은 값을 봐야 합니다.** 정예는 조준·예고 시간이 짧으므로 `rangedAimTime` / `chargerTelegraphTime` 처럼 개체에서 계산한 값을 그리기 쪽에서도 그대로 쓰십시오. 상수를 직접 읽으면 정예의 막대가 끝까지 안 찬 채로 튀어나옵니다
10. `기획.md` **12장(폐기된 안)**을 확인하십시오. 이미 접은 방향을 다시 제안하지 마십시오
11. **패시브 스킬은 페널티가 보너스보다 커야 합니다.** 아래 항목 전체를 읽으십시오. 이 규칙이 그 시스템의 존재 이유입니다

## 패시브 스킬의 제1규칙: 페널티 > 보너스

**아직 구현 전입니다. 만들기 전에 이 규칙부터 읽으십시오** (2026-08-12 사용자 확정).

패시브 스킬은 **개수 제한이 없습니다.** 스탯 고정(3칸)과 달리 원하는 만큼 답니다. 대신 하나하나가 **이득과 대가를 같이** 갖고, 전부 들면 오히려 손해가 되어 스스로 제한이 걸리게 만듭니다. 칸으로 막지 않고 설계로 막는다는 것이 이 시스템의 목적입니다.

**그런데 반대 효과를 짝지어 놓는 것만으로는 그 목적이 성립하지 않습니다.**

- 패시브 A: 지속딜 +30% / 한방딜 -20%
- 패시브 B: 한방딜 +30% / 지속딜 -20%
- 둘 다 들면 → 지속딜 **+10%**, 한방딜 **+10%**

**전부 드는 쪽이 이득입니다.** 곱연산으로 해도 1.3 x 0.8 = 1.04 로 여전히 이득이라 방식의 문제가 아닙니다. 그래서 규칙이 필요합니다.

> **모든 패시브는 페널티의 크기가 보너스보다 커야 합니다.**

- 패시브 A: 지속딜 +40% / 한방딜 **-50%**
- 패시브 B: 한방딜 +40% / 지속딜 **-50%**
- 둘 다 들면 → 양쪽 다 **-10%**

이렇게 해야 **"안 쓰는 축이 있는 빌드만 이득을 본다"**가 됩니다. 하나만 들면 그 축은 +40% 이고, 안 쓰는 축의 -50% 는 나에게 아무 일도 아닙니다. 전부 들면 모든 축에서 손해입니다.

**새 패시브를 추가할 때마다 이 부등호를 확인하십시오.** 보너스가 페널티보다 크거나 같은 패시브가 하나라도 들어가면, 그것만 "무조건 드는 것"이 되어 선택이 사라집니다. 구현할 때 자동 점검(`scripts/smoke.ts`)에 부등호 검사를 반드시 같이 넣으십시오.

**아직 안 정한 것: 무엇을 축으로 삼을 것인가.** 두 안이 있고, 어느 쪽이냐에 따라 **공격 스킬 자체를 손봐야 할 수 있습니다.** 그래서 기획을 먼저 굳히고 구현합니다.

- **(가) 상황 기반**: 주변 적 수, 대상의 남은 체력 비율처럼 **상황**으로 나눕니다. 모든 스킬에 고르게 걸리고 스킬마다 태그를 달 필요가 없습니다. 초안은 군중 제압 / 저격수, 처형자 / 개시자, 역병 / 일격 세 쌍입니다
- **(나) 지속 대 즉발**: 피해의 성격으로 나눕니다. 빌드 정체성이 더 선명하지만 **지금 지속형 공격 스킬이 화염방사기와 오라 둘뿐**이라 공격 3칸을 못 채웁니다. 이 안으로 가려면 지속형 스킬을 더 만들어야 하고, 스킬마다 "지속인가 즉발인가" 태그가 필요합니다(유탄·지뢰처럼 애매한 것이 반드시 생깁니다)

**어느 쪽이든 화면에 지금 배율이 보여야 합니다.** 무제한으로 달 수 있는데 상황마다 다르게 걸리면, 왜 피해가 들쭉날쭉한지 알 수 없게 됩니다. 가중치 상점이 폐기된 이유와 같은 문제입니다. **계산해서 보여줄 수 없는 시스템은 만들지 마십시오.**

## 구현하면서 기획에 없어서 결정한 것

기획대로 만들면 실제로 깨지는 지점이 있어 아래를 추가했습니다. 전부 `balance.ts`에 주석과 함께 있습니다.

- **시간 경과 강화** (`TIME_SCALING`): 적 체력과 공격력이 분당 오릅니다. 없으면 10분 이후가 너무 쉬워집니다
- **보스 체력에는 시간 배율을 적용하지 않음**: 기본공격이 가장 가까운 적을 노리므로 잡몹에 둘러싸인 보스는 실제로 맞는 시간이 짧습니다. 시간 배율까지 겹치면 첫 보스를 못 잡고, 보스가 살아있으면 다음 보스도 안 나옵니다. 등장 횟수에 따른 증가(`hpGrowthPerSpawn`)만 씁니다
- **후반 스폰 가중치에서 초반 적 비중을 낮춤**: 상한 근처에서는 스폰 기회가 드물어서, 후반 적의 가중치만 올려서는 실제 등장으로 이어지지 않습니다

아래는 사용자가 직접 방향을 정해준 것입니다 (2026-08-04).

- **이동은 방향키만, 유틸 스킬은 Q 하나**: 숫자키를 스킬로 쓰니 연타하다가 레벨업 화면에서 실수로 골라버립니다. 이후 유틸을 1칸으로 줄이면서 키도 Q 하나만 남았습니다
- **공격 스킬 자동 발동** (`SkillDef.auto`): 조준이 없어 공격은 손이 갈 이유가 없습니다. 대시·넉백·시간감속만 직접 씁니다
- **스킬 선택 거절 제거 + 리롤** (`REROLL_UPGRADE`): 무조건 하나를 고르되, 상점에서 산 리롤 횟수만큼 다시 뽑습니다 (최대 3회)
- **좌우 정보 패널** (`PANEL`, `VIEW`, `ARENA_X`): 캔버스를 1710으로 넓혀 경기장 밖에 스탯과 스킬을 띄웁니다. 경기장 위에 겹치면 그 자리가 사각지대가 됩니다

- **방패는 무효가 아니라 내구도** (`ENEMY_PARAMS.shield`): 정면 피해를 체력의 50%만큼 방패가 대신 받고, 깨지면 무효화가 사라지는 대신 속도 x1.35. 관통·폭발·전방위 스킬은 방패를 무시합니다(`ignoreShield`). 막히는 것은 기본공격, 산탄, 화염방사기
- ~~**보스 등장 중 스폰율 0.2배**~~ **철회했습니다** (`BOSS.spawnSlow` 5 → 1, 2026-08-04 플레이 확인). "잡몹이 가득하면 기본공격이 보스에 닿지 않는다"는 걱정에서 넣었는데, 실제로는 그 정도로 막히지 않고 보스전만 한산해져서 긴장이 풀렸습니다. 지금은 보스전에도 평소대로 스폰됩니다
- **스폰을 간격이 아니라 초당 마리 수로 표현** (`SPAWN.rateStart/rateMax`): 초당 0.7 → 8분에 2.4에서 멈춥니다. 동시 존재 상한은 60 → 80. 자동 검증에서 8분쯤 상한에 도달해 그 뒤로 유지되고, 14분 안에 15종을 전부 만납니다
- ~~**15분 이후 무한 심화** (`LATE_SCALING`)~~ **없앴습니다** (2026-08-12). 아래 "열 번째 조정" 참고. 시간 강화는 이제 `TIME_SCALING` 하나로 처음부터 끝까지 분당 +0.2 입니다

아래는 첫 플레이 피드백을 받고 결정한 것입니다 (2026-08-04 저녁).

- ~~**난이도 해금 조건은 "15분 도달"**~~ **바뀌었습니다** (2026-08-06). 난이도 선택 화면은 처음부터 열리고, -1 과 0 은 언제나 고를 수 있습니다. 1 부터는 바로 아래 난이도로 요구 시간을 버텨야 차례로 열립니다
- **난이도 효과는 누적이고 쌓는 방식은 합입니다** (2026-08-06 에 곱 → 합으로 바꿨습니다): 단계마다 값을 새로 쓰지 않고 그 단계에서 더해지는 양만 적습니다. **곱하지 마십시오.** 스폰율 +25% 가 두 번이면 +50% 이지 +56% 가 아닙니다. 곱연산이면 표에 적힌 숫자와 실제 결과가 갈라져서, 표를 보고도 최종이 얼마인지 모르게 됩니다. **예외는 `wave` 와 `bomberWave` 둘뿐입니다.** 이것만은 더하지 않고 뒤 단계가 앞을 갈아끼웁니다. "1분마다 5마리"와 "1분마다 15마리"는 동시에 성립하는 성질이 아닙니다
- ~~**난이도 10 이후는 체력·공격력만 무한히 상승**~~ **없앴습니다** (2026-08-06). 난이도는 **15가 끝**입니다. 무한히 오르는 값이 없어야 각 단계에 고유한 장치를 붙일 수 있습니다
- **정예 비율에 상한** (`DIFFICULTY.eliteRatioCap = 0.85`): 전부 정예가 되면 정예를 구분하는 의미가 사라집니다. **난이도 9 이상은 이 상한을 안 씁니다.** 거기서는 의도적으로 전원 정예이며, 대신 표식을 감춥니다
- **난이도 보상 배율** (`DIFFICULTY.coinMulPerLevel`): 없으면 높은 난이도를 고를 이유가 전혀 없습니다. 게임오버 화면에 주운 개수와 배율을 같이 보여줍니다
- **사망 원인 추적** (`KillerInfo`): 접촉뿐 아니라 적탄 · 장판 · 자폭도 주인을 들고 다닙니다. 장판은 깐 적이 이미 죽어 있으므로 장판 자체에 주인을 기록해야 합니다
- **해금 시간은 난이도 3부터 30분**: 이제 상수가 아니라 **표의 한 항목**입니다 (3단계의 `clearTimeAdd: 900`). 클리어 조건도 그 난이도가 주는 효과 중 하나라, 표 밖에 따로 두면 "이 난이도가 무엇을 바꾸는가"를 한곳에서 못 읽습니다. 앞쪽은 가볍게 넘기고 뒤로 갈수록 오래 버티게 합니다. 단계마다 누적으로 늘리는 안은 후반에 한 판이 두 시간을 넘겨서 접었습니다
- **자폭적 심지와 폭발 반경 표시**: 심지는 점화 전에도 항상 그립니다. 실루엣이 기본적과 다르다는 것이 요점입니다. 점화되면 실제 폭발 반경을 원으로 보여줍니다. 반경을 모르면 피할 방법이 없습니다
- **장판적은 팔각형 + 발밑에 장판 범위 미리보기**: 탱커와 같은 육각형이라 겹쳤습니다. 죽을 자리를 미리 보여주니 어디서 잡을지 고를 수 있게 됐습니다
- **원형 5종을 표식으로 분리**: 기본(표식 없는 순수한 원) · 자폭(심지) · 분열(안쪽 원) · 미라(붕대 줄무늬) · 은신(반쪽만 칠한 원)
- **원거리적 탄속 152 → 300** (`ENEMY_PARAMS.ranged.bulletSpeed`): 플레이어 기본 이동속도(205)보다 느려서, 조준선을 보고 뒤로 걸어가기만 하면 탄이 따라오지 못했습니다. **플레이어 이동속도는 레벨업으로 금방 오르므로 기본값보다 조금 빠른 정도로는 부족합니다.** 직선으로 날아오므로 옆으로 비키면 여전히 피합니다
- **적탄에 흰 심 추가**: 탄이 빨라진 만큼 눈에 밟혀야 합니다. 어두운 배경에 붉은 원만 있으면 빠른 탄이 묻힙니다
- **돌진적 돌진을 절대 속도 950으로** (`ENEMY_PARAMS.charger.dashSpeed`): 배회 속도(59)의 4배는 236이라 플레이어와 비슷했습니다. 3초나 예고해놓고 걸어서 피해지면 예고의 의미가 없습니다. 겁쟁이와 같은 이유로 배수가 아니라 절대 속도입니다
- **돌진 예고를 "범위 + 충전 막대"로**: 지나갈 범위를 실제 몸 폭 그대로 띄우고, 머리 위 막대가 다 차는 순간 돌진합니다. 예전에는 선의 진하기만으로 시점을 짐작해야 했습니다
- **돌진 중 잔상**: 950은 한 프레임에 16px씩 가는 속도라 잔상이 없으면 순간이동처럼 보입니다
- **정예 고유 강화 표** (`ELITE_TRAITS`): 정예에 공통으로 걸리는 `ELITE`(체력·공격력·크기) 배율만으로는 정예가 "그냥 단단한 같은 적"입니다. 그 적을 그 적답게 만드는 부분을 종류별로 강화합니다. 현재 원거리(조준 0.5배)와 돌진(예고 0.7배) 둘뿐이고, **앞으로 사용자가 종류별로 계속 채워나갈 자리입니다**
- **보스 3종 순환** (`BOSS_VARIANTS`, `ALL_BOSS_IDS`): 한 종류만 있으면 두 번째 보스부터는 체력만 늘어난 같은 싸움입니다. 포식자(탄막+돌진) → 폭격기(바닥 폭격) → 군체왕(나선 탄막+대량 소환) 순으로 돌아갑니다. 셋은 요구하는 대응이 서로 다릅니다
- **예고 후 터지는 폭발** (`PendingBlast`, 텔레그래프 `incoming`): 예고 표시는 그림일 뿐이라 실제 피해가 없습니다. 폭격기처럼 "지금 예고하고 잠시 뒤 터진다"를 만들려면 별도 목록이 필요합니다. `incoming` 은 시간이 갈수록 진해지고 안이 차오릅니다(기존 `blast` 는 터진 뒤 옅어지는 반대 방향입니다)
아래는 두 번째 조정에서 결정한 것입니다 (2026-08-05).

- **자폭적의 방아쇠에서 "피격"을 뺐습니다**: 기본공격이 자동이라, 사거리에 들어오는 순간 게임이 대신 방아쇠를 당겼습니다. 플레이어가 "지금 터뜨릴까"를 고를 여지가 없으면 그 적은 그냥 시한폭탄입니다. 지금은 **근접**(바로 앞까지 붙으면 점화 → 0.8초 뒤 폭발)과 **처치**(2초 뒤 시체 폭발) 둘뿐입니다
- **점화 중에도 속도를 올리지 않습니다** (`igniteSpeedMul` 2.5 → 1.0): 2.5배로 쫓아오면 0.8초 안에 반경 95를 절대 못 벗어나서 "붙으면 그냥 맞는 적"이 됩니다. 붙은 것을 보고 물러나면 살 수 있어야 합니다
- **시체 폭발은 편을 가리지 않습니다** (`World.explodeAll`, `PendingBlast.hitsAll`): 잡몹 한가운데의 자폭적을 잡으면 그 자리가 통째로 정리됩니다. 대신 내가 그 자리에 남아 있으면 나도 맞습니다. 자폭적이 "위협"이자 "도구"가 되는 지점입니다. 예고 원(`incoming`)을 2초 내내 띄웁니다
- **자폭으로 스스로 사라진 개체는 시체 폭발을 예약하지 않습니다** (`PHASE_SELF_DESTRUCT`): 안 막으면 근접 자폭 한 번에 폭발이 두 번 일어납니다
- **정예 탱커는 체력 3배 + 초당 5% 재생** (`hpRegenRatio`): 체력만 5배로 주면 "오래 때리면 죽는 덩어리"라 시간만 잡아먹습니다. 재생이 붙으면 화력이 재생을 못 넘길 때 아예 못 죽으므로, 잡을지 피할지를 매번 판단하게 됩니다. `hpRegenRatio` 는 배율이 아니라 **초당 최대 체력의 비율**입니다
- **체인은 1타가 아니라 감쇠와 연쇄 사거리를 풀었습니다**: `damage` 를 올리면 그냥 "약한 스나이퍼"가 됩니다. 체인의 정체성은 무리를 상대하는 것인데, 감쇠 0.85 에 사거리 165 라서 정작 무리를 만나도 네 번째 대상이면 61% 로 줄고 조금만 흩어지면 연쇄가 끊겼습니다. 감쇠를 0.92 로 완화하고 사거리는 아예 없앴습니다. 레벨 5 총합이 143 → 199 로 오르고, 그 이득이 "적이 많을 때"에만 생깁니다
- **체인의 연쇄 사거리(`jumpRange`)는 없앴습니다**: 사거리를 두면 성능을 정하는 것이 "적이 몇이나 있는가"가 아니라 "그 적들이 마침 붙어 있었는가"가 됩니다. 무리를 상대하는 스킬이 운에 좌우되는 셈이라, 210 으로 늘려도 실제 플레이에서는 여전히 끊겼습니다. 지금은 **연쇄 횟수만큼 반드시 때립니다.** 맞은 적을 기준으로 가장 가까운 다음 적을 고르며, 남은 대상이 없을 때만 끊깁니다 (`skills/registry.ts` 의 chain)
- **겁쟁이는 30초 뒤 인내가 끝납니다** (`patienceTime`): 배회 속도가 29라 무시하고 도망만 다니면 영원히 방치됩니다. 화면 구석에 쌓이기만 하고 아무 일도 안 일어나면 처치할 이유가 없는 적이 됩니다. 인내가 끝나면 되돌아가지 않고 계속 돌진합니다
아래는 다섯 번째 조정(난이도 재설계)에서 결정한 것입니다 (2026-08-06).

- **난이도 선택은 목록이 아니라 다이얼입니다** (`ui/screens/difficulty.ts`): -1 ~ 15 를 카드로 늘어놓으면 열일곱 줄이 화면을 넘어가고, 무엇보다 후반 난이도는 효과가 열 줄이 넘어서 카드 한 줄(`desc`)에 안 들어갑니다. 가운데 숫자 하나를 좌우로 돌리고 그 아래에 효과를 전부 폅니다. **숫자 단축키는 없앴습니다.** 10~15 는 애초에 한 자리로 표현이 안 되고, 이동은 화살표 하나로 통일하는 편이 규칙이 단순합니다
- **적 종류별 강화는 "OO 능력 강화" 한 줄로 접습니다** (`buffedKinds`, 2026-08-09 추가): 수치든 규칙이든 한 종류에 한 줄입니다. "자폭병 속도 -20%" 는 그 단계가 자폭병을 강화하는 자리인데도 약화로 읽히고, 종류마다 두세 줄씩 늘어나면 판 전체를 바꾸는 항목이 그 사이에 묻힙니다. 세부는 들어가서 겪을 일이라 화면에서 미리 읽을 이유가 없습니다. **예외는 무적 바보적 하나뿐입니다.** 그것은 강화된 적이 아니라 판 내내 남는 못 죽이는 장애물이라, 있는 줄 모르고 들어가면 대응 자체가 달라집니다. `DIFFICULTY_STEPS` 의 `label` 도 같은 규칙으로 씁니다
- **효과 목록은 문자열이 아니라 항목 배열로 돌려줍니다** (`difficultyEffects`): 예전에는 `difficultySummary` 가 한 줄로 이어 붙인 문장을 줬는데, 15단계에서는 그게 스무 항목짜리 한 문단이 되어 읽을 수가 없었습니다. 배율 항목과 규칙을 바꾸는 항목(`device: true`)을 나눠서 화면에서 다르게 그립니다
- **난이도 -1 을 처음부터 열어둡니다**: 예전에는 난이도 화면 자체가 "15분 도달"로 잠겨 있었습니다. 그 상태로 -1 을 추가하면 **가장 쉬운 난이도가 가장 어려운 조건 뒤에 잠기는** 셈이라, 정작 그것이 필요한 사람은 절대 못 씁니다. 지금은 화면이 처음부터 열리고 -1·0 만 보입니다. 해금 사슬은 0 에서 시작합니다
- **난이도는 15가 끝입니다** (`DIFFICULTY.max`): 무한 심화(`beyondTableMul`)를 없앤 이유는 "체력·공격력만 영원히 오르는 구간"이 사실상 같은 판의 반복이기 때문입니다. 끝이 정해져 있어야 단계마다 고유한 장치를 붙일 수 있고, 15가 실제로 목표가 됩니다
- **웨이브만은 곱하지 않고 갈아끼웁니다** (`wave` / `bomberWave`): 다른 항목은 전부 누적이라 6단계의 "1분마다 5마리"에 11단계의 "1분마다 15마리"를 곱하면 뜻이 없는 값이 나옵니다. 웨이브는 하나의 규칙이지 배율이 아닙니다. `applyStep` 에서 이 둘만 대입으로 처리합니다. **새 항목을 넣을 때 "이건 곱해지는가 갈아끼워지는가"를 먼저 정하십시오**
- **적탄 속도 배율은 `world.addProjectile` 한 곳에서 겁니다**: 적탄을 쏘는 곳이 원거리적·바보적·분열적·보스 셋으로 흩어져 있어서, 각자 곱하게 두면 반드시 하나를 빠뜨립니다. `friendly === false` 인 탄에만 걸리므로 내 탄은 영향이 없습니다. 장판 지속(`addHazard`)도 같은 이유로 한 곳에서 겁니다
- **접촉 피해는 `damageMul` 과 별개입니다** (`contactDamageMul`): 난이도 15 의 "충돌 데미지 +50%" 는 몸에 닿았을 때만입니다. `e.damage` 자체를 올리면 그 적이 쏘는 탄과 남기는 장판까지 같이 올라서 "접촉"이라는 조건이 사라집니다. `collidePlayer` 에서만 곱합니다
- **난이도 9 는 전원 정예이므로 정예 표식을 감춥니다**: `CLAUDE.md` 의 "정예는 시각적으로 반드시 구분한다"를 되돌린 것이 아닙니다. 그 규칙은 "저건 다르다"를 알리기 위한 것인데 전부 정예면 알릴 차이가 없고, 화면 전체가 붉은 링으로 덮여서 오히려 아무것도 안 보이게 됩니다
- **전원 정예화에는 안전장치가 하나 붙습니다**: 정예 확정 코인 드랍을 끕니다(안 끄면 모든 적이 코인을 떨어뜨려 난이도 배율 x2.35 와 겹쳐 폭증). `w.diff.allElite` 로 갈립니다. ~~분열적 재분열도 같이 껐었지만~~ **2026-08-11 에 되돌렸습니다.** 화면이 분열체로만 덮이는 것을 걱정한 장치였는데, 난이도가 올랐는데 그 적만 약해지는 셈이라 앞뒤가 안 맞았습니다. 지금은 난이도 9 이상에서도 정예 분열적이 1 → 3 → 9 로 나뉩니다
- **무적 바보적은 판 시작에 딱 한 마리입니다** (`World.spawnImmortalFool`): 스폰 표에 넣지 않는 이유는 처치가 불가능해서입니다. 계속 나오면 시간이 갈수록 동시 등장 상한을 바보적이 통째로 차지해 다른 적이 아예 안 나오는 판이 됩니다. 적이라기보다 **판 내내 남는 움직이는 장애물**입니다. 체력바도 안 그립니다. 안 줄어드는 막대는 오해만 만듭니다
- **무적 바보적의 세 가지는 한 묶음입니다** (2026-08-12). 하나만 만지면 성격이 통째로 무너지므로 같이 보십시오
  - **타겟이 안 됩니다** (`targetable: !immortal`, `spawnEnemy`). 자동 조준이 못 죽이는 적을 물면 그동안 나가는 화력이 통째로 버려지는데, 조준 조작이 없어서 플레이어가 대상을 바꿀 방법도 없습니다. 투사체도 그냥 통과하므로 몸으로 탄을 막지도 못합니다. **영구 무적이면 타겟에서 빼는 것이 규칙이지 이 적만의 예외가 아닙니다**
  - **난이도 15 의 전방위 8갈래 규칙에서 빠집니다.** 이 한 마리만 **플레이어 쪽 5갈래 부채꼴**을 **절반 탄속**으로 쏩니다. 못 죽이는 적이 사방으로 뿌리면 피할 자리가 아니라 서 있을 자리가 없어집니다. 느린 탄이라 보고 비킬 수 있습니다. 일반 바보적은 8갈래 그대로입니다
  - **이동속도 1.5배.** 위 둘의 대가입니다. 타겟도 안 되고 탄도 느려지면 그냥 무시하고 지나칠 수 있는 적이 되는데, 그러면 "판 내내 남는 장애물"이라는 존재 이유가 사라집니다
  - 수치는 `ENEMY_PARAMS.fool` 의 `immortal*` 네 개이고, 규칙은 `scripts/smoke.ts` 8-2번이 자동으로 잡습니다. 갈래 수와 탄속은 **상수를 읽지 않고 실제로 쏘게 해서** 잽니다
- **영구 무적은 `Enemy.immortal` 로 따로 둡니다**: `invuln` 은 카운트다운이라 "영원히"를 큰 수로 흉내 내면 결국 풀립니다. `damageEnemy` 첫 줄에서 `invuln` 과 같이 막습니다
- **돌진적 쿨타임 제거는 예고를 건드리지 않습니다** (난이도 13): 예고(3초)까지 줄이면 "보고 피한다"가 불가능해져서 그냥 무작위 즉사가 됩니다. 없앤 것은 돌진 후 배회 시간뿐이고, 벽에서 `wallClearance` 만큼 걸어 나오는 구간은 남아 있어서 완전한 무한 연타도 아닙니다
- **분열 탄막은 자식이 흩어지는 각과 어긋나게 쏩니다** (`splitter.burstCount = 6`, 자식은 3): 같은 방향이면 분열체 뒤가 안전지대가 되어 탄막을 넣은 의미가 없어집니다
- **보스 코인은 전체 코인 배율과 따로 갑니다** (`bossCoinPerLevels` / `bossCoinMul`): 난이도 3단계마다 +20%p 가 붙어서, 높은 난이도에서는 "보스를 잡느냐"가 보상의 핵심이 되게 했습니다. 이것도 다른 배율과 같이 **합**입니다. 처음에는 3단계마다 x1.2 를 곱해서 15단계 x2.49 였는데, 난이도 표 전체를 합산으로 통일하면서 같이 맞췄습니다 (x2.00). 한 시스템 안에 곱과 합이 섞여 있으면 어느 쪽인지 매번 코드를 열어봐야 합니다

아래는 네 번째 조정에서 결정한 것입니다 (2026-08-05).

- **분열체는 정예라도 코인을 안 줍니다**: 정예 분열적은 1 → 3 → 9 로 늘어나므로, 개체마다 확정 드랍을 주면 한 마리에서 코인이 13개 쏟아졌습니다. 보상은 "정예 한 마리를 잡았다"에 붙는 것이지 몸통 수에 붙는 것이 아닙니다 (`killEnemy` 의 `e.elite && !e.child`)
- **예고에는 주인이 있습니다** (`Telegraph.owner`): 주인이 죽으면 `killEnemy` 가 그 예고를 같이 걷습니다. 차지 도중에 잡은 돌진적의 경로가 남아 있으면 오지도 않을 돌진을 계속 피하게 됩니다. **적이 자기 자리에 띄우는 예고에는 `owner: e.id` 를 넣으십시오.** 자폭적의 시체 폭발 예고는 예외입니다. 그것은 죽은 뒤에 일어날 일이라 주인이 없어야 맞습니다
- **정예 능력은 전부 `ELITE_TRAITS` 한 곳에 선언합니다**: 배율은 `eliteStatMul`(체력·속도·공격력·크기) / `eliteMul`(그 밖의 배율), 켜고 끄는 것은 `eliteHas`, 값 자체를 갈아끼우는 것은 `eliteValue` 로 읽습니다. 행동 파일에 `if (e.elite)` 를 직접 쓰지 마십시오. 한 곳에서 표로 읽히지 않으면 종류별 차이를 손볼 수가 없습니다
- **장판 피해는 초당이 아니라 틱입니다** (`hazardTick` 0.5초, `hazardArm` 1초): 초당으로 주면 스친 한 프레임에도 소수점 피해가 들어가서 "밟았다"와 "지나갔다"가 구분되지 않습니다. `arm` 은 물러날 유예입니다. 잡자마자 아프면 잡을 이유가 사라집니다
- **장판 피해는 무적 시간을 존중합니다**: 대시로 빠져나가는 것이 장판의 정답이어야 하는데, 무적을 뚫으면 유일한 탈출 수단이 무력해집니다. 예전에는 `hazardDps: 0` 이라 이 문제가 드러나지 않았습니다
- **소환 간격은 `timer3`, 배회는 `timer`**: 같은 칸을 두고 다투면 wander 가 계속 다시 채워서 소환이 영영 안 걸립니다. **`wander()` 를 쓰는 적에 주기 행동을 붙일 때는 반드시 다른 타이머 칸을 쓰십시오**
- **적 무적은 `Enemy.invuln`** (군체왕 분노): `targetable` 로 대신하면 스킬 타겟에서만 빠지고 폭발 피해는 그대로 들어갑니다. `damageEnemy` 첫 줄에서 막습니다. 화면에는 링과 "무적" 글자로 표시합니다. 안 보이면 "왜 피가 안 깎이지"가 됩니다
- **군체왕은 잡몹 수가 곧 세기입니다**: 근접과 탄막을 다른 둘보다 약하게 잡는 대신, 잡몹 12마리마다 나선 갈래가 하나씩 붙고 20마리를 넘기면 주변을 삼켜 삼킨 수 x2 발의 탄막으로 뱉습니다. 삼켜진 잡몹은 `killEnemy` 를 거치지 않으므로 **경험치도 코인도 안 줍니다.** "내가 치울 것인가, 보스가 먹게 둘 것인가"의 선택입니다
- **보스 체력 2배는 위험이 있습니다**: 보스는 하나가 살아 있는 동안 다음이 나오지 않습니다(`bossAlive`). 못 잡으면 그 뒤로 보스가 영영 끊기고 코인·회복도 같이 끊깁니다. `scripts/smoke.ts` 1번이 14분 자동 플레이에서 보스 등장 횟수와 남은 체력을 찍으니, 체력을 더 올릴 때는 그 숫자를 보십시오

아래는 세 번째 조정에서 결정한 것입니다 (2026-08-05).

- **지속형 스킬에 "다시 켜기"를 두지 않습니다**: 회전 궤도가 쿨 6초 / 지속 5초였는데, 쿨다운 감소를 조금만 올리면 쿨이 지속보다 짧아져 구체가 겹쳐 쌓였습니다. 지속시간을 늘리는 쪽으로 강화되는 스킬은 결국 전부 이 지점에 닿습니다. 켜고 끄는 개념 자체를 없애고 **항상 유지**로 바꿨습니다. `activate` 는 매 프레임 불리지만 "개수가 맞으면 아무것도 안 하고 false 를 돌려주는" 형태라 쌓이지 않습니다. 대신 공격력을 예전 가동률만큼 낮춰 잡습니다
- **선택지 확률은 카드마다 굴리면 안 됩니다** (`MAX_UPGRADE_CHOICES_PER_ROLL`): `upgradeOfferChance: 0.35` 를 카드 3장에 각각 굴리면 한 장이라도 뜰 확률이 1 - 0.65³ = 72% 입니다. 숫자만 보면 35% 인데 체감은 확정이었습니다. 판마다 한 번 굴려서 걸리면 정해진 장수만 섞습니다. **새 확률 손잡이를 넣을 때는 "무엇에 대한 확률인가"를 먼저 정하십시오**
- **적의 벽 판정은 위치가 아니라 방향으로 합니다**: 돌진적이 벽에 닿은 채로 예고를 마치면, 반대쪽으로 튀어나가는 첫 프레임에 `e.x <= radius + 2` 가 참이라 곧바로 자기 기절에 걸렸습니다. 벽 근처의 돌진적은 3초를 예고하고 아무 일도 못 하고 끝났습니다. 지금은 속도 부호까지 함께 봅니다. 더해서 `wallClearance` 안쪽이면 예고를 시작하지 않고 안쪽으로 걸어 나옵니다
- **예고는 진하기가 아니라 채우기로 알립니다**: "조금 진해졌다"는 매번 이전 프레임과 견줘야 알 수 있어서 시점 판단에 못 씁니다. 돌진 통로는 적 쪽에서부터 실제로 차오르고, 다 차는 순간 튀어나옵니다. 자폭적 시체 폭발(`incoming`)과 같은 방식입니다
- **화면 흔들림은 부르는 곳이 아니라 `SHAKE` 한 곳에서 조절합니다**: 흔들림을 부르는 곳이 18군데인데 값이 더해지기만 해서, 폭발과 처치가 몰리는 후반에는 화면이 계속 떨렸습니다. 세기의 비율은 그대로 두고 배율(0.3)과 상한(9)을 한 곳에서 겁니다. **새로 `addShake` 를 부를 때 값을 키워서 맞추지 말고 이 상수를 보십시오**
- **레벨업 알림은 `World.notices` 줄에 담아 순서대로 띄웁니다**: 원인은 메뉴가 떠 있어도 `world.effects.update` 는 계속 도는 데 있었습니다(`main.ts` 의 `screen !== 'playing'` 갈래). 스탯 글자는 수명이 0.72초라 스킬 선택창을 보는 동안 그대로 수명을 다했습니다. 반면 `world.update` 는 멈추므로, 알림을 이 줄에 담아두면 창이 닫힌 뒤부터 시간이 흐릅니다. **머리 위에 순서를 두고 띄울 것이 생기면 `effects.text` 를 직접 부르지 말고 `queueNotice` 를 쓰십시오.** 뒤에 이어 붙일 때는 `noticeTail()` 을 더합니다
- **도감이 흐리던 원인**: 도감 항목이 누를 수 없는 카드라 `disabled` 를 걸었는데, 그 스타일이 상점의 "못 사는 항목" 흐림(`opacity: 0.45`)이었습니다. 읽으라고 만든 화면 전체가 45%로 흐려지고 있었습니다. `card({ info: true })` 를 쓰면 또렷하게 나옵니다. **누를 수 없는 정보 카드에는 `info: true` 를 같이 넘기십시오**

## 파일 구조

`기획.md` 3장 구조를 그대로 따랐습니다. 추가된 것만 적습니다.

- `src/game/types.ts` 엔티티 인터페이스 모음 (순환 참조를 피하려고 분리)
- `src/game/debug.ts` F1 오버레이와 단독 소환
- `src/enemies/update.ts` 적 상태이상 → 행동 → 분리 → 이동
- `src/render/scene.ts` 월드 그리기 (renderer.ts는 그리기 원시 연산만)
- `src/ui/screens/*.ts` 메뉴는 DOM으로 그립니다 (캔버스는 게임 화면만)
- `src/meta/difficulty.ts` 난이도 표를 배율 묶음으로 바꾸는 계산 (표 자체는 `balance.ts`)
- `scripts/smoke.ts`, `scripts/boot.ts` 브라우저 없는 점검
- `scripts/survival.ts` 회피 봇 생존 시간, `scripts/levelcurve.ts` 레벨 곡선과 스킬 선택 횟수

## 작업 방식

- 한국어 존댓말로 대화합니다
- 한국어 문장에 대시(ㅡ, —)를 쓰지 않습니다
- 기획과 다른 방향을 제안할 때는 먼저 짚고 확인을 받습니다
- 사용자 의견에 무조건 동의하지 말고 문제가 보이면 근거를 들어 반론합니다
